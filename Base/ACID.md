Стандарт того как правильно должны работать гарантии в базе данных для реализаций гарантий.

# A - Atomacyty (Атомарность)
- Транзакция в база данных является единным блоком и реализуется или полностью или не реализуется вообще. Если какой то оператор терпит неудачу транзакция не выполняется.
# С - **Consistency** (Согласованность)

- **Данные всегда должны оставаться правильными и соответствовать правилам.**

Если есть ограничения:
- баланс не может быть отрицательным
- email должен быть уникален
- внешний ключ должен существовать
    
…то они всегда соблюдаются.

👉 Если операция нарушает правило — транзакция не совершится.
# I — **Isolation** (Изоляция)
**Одновременные транзакции не должны мешать друг другу.**

Если два человека переводят деньги одновременно, они не должны «перепутать» данные.

Изоляция делает так, чтобы каждая транзакция видела «свою» логичную картину данных.

[[Уровни изоляции]]
# D — **Durability** (Надёжность)

**Если база сказала COMMIT — данные уже никогда не пропадут.**

Даже если:

- выключили свет
    
- сервер упал
    
- машина перезагрузилась
    

Данные сохраняются благодаря журналированию (WAL).


**Применение**:

**Пример**:

**Доп. ссылки:** [[Транзакции]]

**Tags**: #IT #Backend  #БазаДанных 