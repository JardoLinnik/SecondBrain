**RPC (Remote Procedure Call)** — это **удалённый вызов процедуры (или функции)**,  
то есть способ, при котором программа **может вызвать функцию на другом сервере**,  
как будто она находится у неё “локально”.

Работает через сеть, но для разработчика выглядит как обычный метод.

⚙️ Быстрее и удобнее REST внутри системы,  
но требует контрактов и строгой типизации.


Как это работает под капотом
	1. **Клиент вызывает функцию**  
    → библиотека RPC превращает это в сетевой запрос (например, HTTP, TCP).
	2. **Сервер получает запрос**, распознаёт, какую функцию вызвать.
	3. **Функция выполняется** на сервере.
	4. **Результат сериализуется** (в JSON, Protobuf и т.д.) и отправляется обратно.
	5. **Клиент получает ответ**, десериализует его и возвращает результат.

# Cтруктура RPC-системы

|Компонент|Что делает|
|---|---|
|**Client stub**|Обёртка, превращающая локальный вызов в сетевой запрос|
|**Server stub**|Обратная часть — принимает запрос и вызывает реальную функцию|
|**Transport**|Канал связи (HTTP, TCP, gRPC, WebSocket и т.п.)|
|**Serialization**|Как кодируются данные (JSON, Protobuf, Avro и т.д.)|

**Применение**:

## Где применяют RPC

|Сценарий|Почему подходит|
|---|---|
|🧱 Внутренние микросервисы|gRPC/Thrift — эффективный двоичный протокол|
|🧩 Backend ↔ Backend коммуникация|Быстро, типобезопасно|
|📈 Высоконагруженные системы|Меньше оверхеда, лучше производительность|
|🔐 Закрытые внутренние сети|Не нужно человекочитаемое API|
|🧠 Real-time системы|gRPC streaming, WebSocket RPC|

**Пример**:
## 🌍 Популярные реализации RPC

|Технология|Формат данных|Транспорт|Особенности|
|---|---|---|---|
|**gRPC**|Protocol Buffers (protobuf)|HTTP/2|Быстрый, поддерживает стриминг, типизация|
|**Thrift (Apache)**|Binary / JSON|TCP / HTTP|Поддерживает много языков|
|**JSON-RPC**|JSON|HTTP / WebSocket|Простой, человекочитаемый|
|**XML-RPC**|XML|HTTP|Старый, часто в enterprise|
|**Dubbo (Alibaba)**|Custom binary|TCP|Очень популярен в Китае|
|**Avro RPC**|Avro|HTTP / Kafka|Часто в big data (Hadoop, Kafka)|

**Доп. ссылки:** [[API]]

**Tags**: #Backend #IT #АрхитектуаПриложения 