
Наследование ключевой принцип ООП. Позволяет классам создаваться на основе уже существующего.
Новый класс берет в себя свойства и методы родительского и может изменять или добавлять ноый функционал.

Очень схож с паттерном [[Композиция]], но отличается по своей логике.
Нследование это про родство, а композиция про содержание.

- Уменьшает дублирование
- Код чище
- Без общих интерфейсов.

# Множественное наследование — что это?

**Множественное наследование** — это когда класс наследует **несколько родительских классов одновременно**:

```
class A:
    pass

class B:
    pass

class C(A, B):   # множественное наследование
    pass

```

В Python это позволяет:

### ✔ комбинировать функциональность разных классов

Например, объект может быть и «логируемым», и «конфигурируемым».

### ✔ использовать mixins (важно в backend)

# Что такое MRO (Method Resolution Order)

**MRO — порядок поиска методов** в системе наследования.

Python использует алгоритм **C3-линеаризации**, который:
- обеспечивает предсказуемый порядок,
- предотвращает конфликты,
- гарантирует, что каждый класс появляется один раз.
    

Посмотреть MRO можно так:

`print(C.__mro__)`
или
`help(C)`

📌 **Python идёт сверху вниз, слева направо.**
Сначала идут дети а потом деды

Например:

- `CreateMixin`
- `UpdateMixin`
- `DeleteMixin`
- `AuthRequiredMixin`

**Применение**:
- ORM
- Фреймворки
- Логирование

Когда логика родственная
- `Dog` **является** `Animal`
- `AdminUser` **является** `User`
- `JsonResponse` **является** `HttpResponse`

**Пример**:

**Доп. ссылки:** [[ООП]]

**Tags**: #IT #Backend  #OOP