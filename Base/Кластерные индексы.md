**Кластерный индекс (clustered index)** — это такой индекс, при котором **данные таблицы физически хранятся в отсортированном порядке по ключу индекса**.  
То есть сама таблица _становится B-Tree деревом_, где листья содержат не ссылки, а **сами строки**.

Объясняю максимально понятно 👇

---

# 🧩 Простое объяснение кластерного индекса

Обычно в таблице данные лежат «как попало».  
Но если есть кластерный индекс — таблица хранится **в отсортированном порядке по этому полю**:

Например, таблица:

`orders(id, user_id, amount)`

Если создаём кластерный индекс по `id`, то данные будут физически храниться так:

`id: 1 → строка id: 2 → строка id: 3 → строка ...`

**Ключ = порядок хранения строк.**

---

# 🧱 Как это работает технически

Кластерный индекс — это B-Tree, где:

- листья → **строки таблицы**
    
- внутренние узлы → ключи для поиска
    

        `[10]       /     \    [1..9]   [10..20]      |         |   (строки)  (строки)`

То есть не нужно искать в индексе и потом идти в таблицу — данные уже **внутри** дерева.

---

# 🥇 Главные преимущества кластерного индекса

### ✔ 1. Очень быстрый поиск по первичному ключу

Индекс → сразу запись (без дополнительного lookup).

### ✔ 2. Очень быстрые диапазонные запросы

`WHERE id BETWEEN 100 AND 200`

Строки хранятся подряд → минимум дисковых операций.

### ✔ 3. Таблица автоматически упорядочена

Сортировка по ключу часто ускоряется «бесплатно».

### ✔ 4. Один раз упорядочили — потом получаем выигрыш

---

# ⚠ Ограничения

### ❗ В таблице может быть только **один** кластерный индекс

Потому что таблица физически упорядочена _только одним способом_.

### ❗ Медленные UPDATE по ключу

Меняет ключ → строку нужно перемещать в другое место.

### ❗ Медленные INSERT в середину дерева

Если вставка «внутрь», нужно раздвигать страницы, делать split.

### ❗ Вторичные индексы становятся "тяжелее"

Потому что вторичные индексы хранят ссылки на кластерный ключ.

---

# 🧩 Где используются кластерные индексы?

## ⭐ **MySQL (InnoDB)**

— таблица всегда _кластеризована по PRIMARY KEY_.  
Если PK нет → InnoDB создаёт скрытый кластерный ключ.

## ⭐ **SQL Server**

Прямо управляемый кластерный индекс:

`CREATE CLUSTERED INDEX idx ON table(id);`

## ⭐ **PostgreSQL**

Не имеет настоящих кластерных индексов,  
но есть команда:

`CLUSTER table USING indexname;`

Она _разово_ перезаписывает таблицу по индексу,  
но порядок **не поддерживается автоматически**.

## ⭐ **SQLite**

Не имеет кластерных индексов.

---

# 🔍 Что происходит при SELECT?

### Без кластерного индекса:

1. Идём в обычный индекс → получаем ссылки на строки
    
2. Для каждой строки отдельно читаем данные из таблицы
    
3. Делаем много случайных I/O
    

### С кластерным индексом:

1. Идём в индекс и прямо в листьях получаем строку
    
2. Никаких дополнительных seek-ов
    

**Особенно эффективно на HDD/SSD.**

---

# 📝 Примеры, где кластерный индекс идеален

### ✔ первичные ключи `id` (int, bigint)

### ✔ автоинкрементные ID

### ✔ Timestamp, если таблица логов

### ✔ GUID (если сортируется, например UUID v7)

### ✔ частые диапазонные запросы:

`created_at BETWEEN ...       id > ...`

---

# 🚫 Когда кластерный индекс вреден

### ❌ GUID v4 (случайный) → фрагментация

### ❌ частые UPDATE ключей

### ❌ объекты с переменными размерами строк

### ❌ OLTP с хаотичными вставками

---

# 🎯 Итог

**Кластерный индекс — это структура, в которой таблица _сама хранится в виде B-Tree по ключу индекса_.**

Плюсы:

- быстрый поиск и диапазоны
    
- данные отсортированы
    
- минимум I/O
    

Минусы:

- дорогие вставки в середину
    
- можно создать только один
    
- вторичные индексы становятся тяжелее
    

Используется в:

- MySQL InnoDB (по умолчанию)
    
- SQL Server (явно)
    
- PostgreSQL (в виде one-time cluster)

**Применение**:

**Пример**:

**Доп. ссылки:**

**Tags**: #IT #Backend  #БазаДанных 