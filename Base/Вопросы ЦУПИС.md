- 1 Нагрузку измеряли через Locust и Prometheus.

	На стейдже поднимали полную копию сервиса, прогоняли сценарии API до упора,  
	фиксировали latency и error rate.

	2100 RPS — это устойчивая нагрузка, при которой p95 < 200 мс и ошибок < 1 %.

	Метрики снимались из Prometheus через Grafana, чтобы подтвердить стабильность под нагрузкой.

- 2. 
	Писал запросы разной сложности — от стандартных CRUD до достаточно сложных аналитических,  
	с несколькими `JOIN`, `GROUP BY`, подзапросами и оконными функциями.

	Использовал их для агрегаций, отчётов и вычислений метрик в продакшене — особенно в задачах мониторинга и антифрода.

	Иногда приходилось оптимизировать тяжёлые запросы через индексы, `EXPLAIN ANALYZE` и денормализацию данных.

- 3 Расскажи о нормализации и денормализации
	Нормализация — это способ организации данных так, чтобы **не было избыточности и дублирования**,  
	а денормализация — наоборот, **осознанное дублирование** ради **скорости чтения и уменьшения количества JOIN’ов**.

	В проектах я использовал обе стратегии:  
	нормализованные схемы — для транзакционных данных,  
	денормализованные — для аналитики и высоконагруженных чтений (например, витрины остатков или отчёты).
4. Group by и having как работали
- `GROUP BY` — **группирует строки** по одному или нескольким полям.
    
- `HAVING` — **фильтрует уже сгруппированные данные** (в отличие от `WHERE`, который фильтрует строки _до_ группировки).
- 5 СТЕ
	**CTE (Common Table Expression)** — это **временный набор данных**,  
	который ты создаёшь внутри запроса с помощью ключевого слова `WITH`,  
	чтобы потом использовать его в основном `SELECT`, `UPDATE`, `DELETE` и т.д.
4. 
	1. **Оконные функции** — это функции, которые вычисляют значение **по набору строк ("окну")**,  
	при этом **не сворачивая** результат в одну строку, как `GROUP BY`.
	То есть — можно посчитать агрегаты, ранги, суммы и т.д.  
	**для каждой строки**, сохранив при этом все данные.
- 5.race condition
	Race condition — это состояние гонки,  
	когда несколько транзакций одновременно изменяют одни и те же данные,  
	и результат зависит от порядка их выполнения.

	В продакшн-системах такие ситуации решаются с помощью транзакций, блокировок (`FOR UPDATE`),  
	атомарных операций или сериализуемого уровня изоляции.

	В моих проектах (например, Inventory Service) я сталкивался с race condition при обновлении остатков,  
	решал это через транзакции и ключевую маршрутизацию событий в Kafka.

6. 
	### Функциональные:

- Что конкретно система должна делать?
    
- Какие входные и выходные данные?
    
- Какой SLA по задержкам / доступности / точности?
    

### ⚙️ Нефункциональные:

- Нагрузка (RPS, объем данных, пиковые часы).
    
- Ограничения по времени отклика.
    
- Масштабируемость, fault tolerance, мониторинг.
    
- Требования к безопасности (шифрование, доступ, анонимизация).
    

> 💬 _“Например, при проектировании витрины остатков уточнял: сколько SKU, с какой частотой обновляются данные, нужно ли хранить историю.”_

- Расскажи об архитектурных достижениях
- Как вы их решали
- Про рпс
- про антифрод побольше
- про вычисление остатков в реальном времени
- про кеш
- про базы побольше
- что именно ты делал 
- по архитектуру проекта 
- про команду
- про взаимодействие с командой
- про ci cd
- про эндпоинты
- про паттерны
- про адаптеры
- про архитектуру пиложения уведомлений
- про сбор метрик
- как разработать приложени при нефункциональном требовании в тысячи рпс
- нефункциональные требования
- время ответа на каждый запрос 
- логика системы остатков
- как именно делались вызовы и не блокировались между собой
- как работала кафка в этой системе 
- синхронное и ассинхронное взаимодействие
- что обеспечивало 2100 рпс
- балансировка нагрузки между подами в кубере
- crud
- что предоставлял эндпоинт
- обьем даннах в бд
- партицирование шардирование
- воркеры
- индексы
- как использовался кеш 
- что происходит при запросе на создание сущности 
- проверяется ли в бд количество обьектов
- какие технологии спользует т банк отдельно 
- что такое индекс и как он работает
- составные индексы, частичные индексы как работают с записью
- возможно ли подружиь шардирование и партицирование и какую выгоду это даст
- шард и партиция
- кубер элементы 
- как настроить поиск при шардировании и партицировании
- как партицирование взаимодействует с ключами платежа
- абстракциия, структура проекта 
- redis распределенный замок
- semaphore gather
- разворот связного списка 
- бизнесово почитат про т город
- посмотреть антона 
- что делали эндпоинты 
- Резюме разобрать и накинуть вопросы
- список контактов
- 