
**B-Tree индекс** — это структура данных, которую используют почти все реляционные базы (PostgreSQL, MySQL, SQLite) для **быстрого поиска строк**.  
Она похожа на «умное дерево», которое всегда остаётся сбалансированным и позволяет находить данные за **логарифмическое время O(log n)**.

Объясню максимально просто и понятно.

---

# 🧩 Что такое B-Tree индекс простыми словами

Представь обычный телефонный справочник:

- фамилии идут **в отсортированном порядке**
    
- чтобы найти человека, ты не листаешь все страницы подряд
    
- ты открываешь середину → приближаешься к нужному месту → находишь быстро
    

B-Tree делает **то же самое**, только внутри базы данных.

Индекс хранит отсортированные значения колонок:

`10 15 20 25 30 35`

и позволяет очень быстро понять, где находится нужная строка.

---

# 🏗 Как устроено B-Tree (очень простая схема)

B-Tree — это **многоуровневое дерево**:

                `[20]               /      \         [10,15]      [25,30,35]`

- В **корне** лежат ключи, которые распределяют диапазоны
    
- У каждой ветви — свои отсортированные ключи
    
- Каждая запись знает, где находится следующая часть поиска
    

**База никогда не делает полный перебор**, а «спускается» по дереву, как по GPS.

---

# 🔍 Как работает поиск

Допустим, ты ищешь значение **30**:

1. Сравниваешь с корнем → 30 > 20 → идём вправо
    
2. В правом узле: 25 < 30 < 35 → нашли!
    
3. Узел содержит ссылку на строку в таблице
    

Всё — поиск завершён за 2 шага, даже если строк миллионы.

---

# ⚡ Почему B-Tree быстрый?

Потому что:

- дерево **сбалансировано**
    
- глубина маленькая (обычно 3–4 уровня, даже для миллионов строк)
    
- на каждом шаге ты отбрасываешь половину вариантов (как бинарный поиск, но с группой ключей)
    

Результат:

> поиск, вставка и удаление работают примерно за **O(log n)**.

---

# 📌 Что хранится в B-Tree индексе?

Каждая запись содержит:

- **ключ** (значение колонки)
    
- **ссылку на строку** в таблице (row pointer или TID)
    
- иногда — другие данные
    

Пример для таблицы:

|id|name|
|---|---|
|1|Ann|
|2|Bob|
|3|Carl|

Индекс по name хранит:

`Ann  → pointer(row1) Bob  → pointer(row2) Carl → pointer(row3)`

---

# 🎯 Когда B-Tree индекс помогает?

### ✔ Очень быстрые точечные запросы:

`SELECT * FROM users WHERE id = 123;`

### ✔ Поиск по диапазону:

`WHERE age BETWEEN 18 AND 30`

### ✔ Сортировка:

`ORDER BY created_at`

### ✔ JOIN по ключу:

`ON users.id = orders.user_id`

### ✔ LIKE с префиксом:

`WHERE name LIKE 'Alex%'`

---

# 🚫 Когда B-Tree не работает или бесполезен?

### ❌ LIKE с шаблоном в начале:

`WHERE email LIKE '%gmail.com'`

### ❌ Поиск по неравенству:

`WHERE salary != 50000`

### ❌ Колонка часто меняется

Например, big counters — индекс будет постоянно обновляться.

### ❌ Таблица маленькая

Индекс не нужен — полное сканирование быстрее.

---

# 🤓 Мнемоника для запоминания

> B-Tree индекс — это «телефонный справочник» внутри БД,  
> который ускоряет поиск, сортировку и поиск по диапазону,  
> благодаря дереву с минимальной высотой.

---

# 🧠 Итог

**B-Tree индекс — это сбалансированное многомерное дерево, хранящее отсортированные ключи, которое позволяет очень быстро находить и фильтровать данные.**

Он даёт огромный прирост скорости при:

- поиска по равенству
    
- поиске по диапазонам
    
- сортировках
    
- join-ах
    

Используется почти во всех SQL базах.

---



**Применение**:

**Пример**:

**Доп. ссылки:** [[Индексы]]

**Tags**: #IT #Backend  #БазаДанных 