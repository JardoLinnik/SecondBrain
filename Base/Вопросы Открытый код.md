
- Ассинхронные генераторы в Python? И какие предметные области по применению.

	- Это обычный генератор  с таким же ключевым словом yeald возвращающий значения по мере итерации, но работающий в ассинхронном контексте. В отличие от обычного генератора не блокирует нам поток до получения следующего значения.

	- Я применял их когда нужно было работать с потоком данных при чтении событий с kafka, оборачивал это все в ассинхронный генератор и тем самым постепенно читал события путем `async for`

	- При работе с API чтобы клиенту не отдавать сразу большой обьем данных это все тоже оборачивалось в ассинхронный генератор и через `StreamingResponse`в FastApi  клиент получал данные по мере готовности, а не сразу весь ответ.
- С точки зрения API Python расскажи в чем разница между ассинхронным и синхронным генератором.
	- Синхронный генератор заблокирует нам поток пока не будет полученно следующее значение.
	- Нотации генератора.
	- Поддержка метода .send(Value)
- Метода .send(value) у генератора
	- У генераторов помимо `next()` есть метод `.send()`. Он позволяет не только вытаскивать значения наружу, но и отправлять данные обратно внутрь генератора. Таким образом генератор превращается в корутину: на `yield` можно не просто остановиться, но и принять значение.
	- В моём проекте я мог бы использовать это, если нужно было бы делать двунаправленную обработку потока данных, например, получать события от брокера и сразу передавать контекст внутрь генератора для последующей логики.
- Компиляция кода на Python
	### 1. Базовый уровень

	- ✅ **Компиляция в байткод** (`.py → .pyc`) — происходит автоматически.  
	    🔻 Минус: легко дизассемблируется.
	### 2. Средний уровень

	- ✅ **Упаковка в бинарь** (PyInstaller, Nuitka, cx_Freeze)  
	    ➝ код не лежит «открыто», заказчик видит только исполняемый файл.
    
	- ✅ **Обфускация** (PyArmor, Nuitka options)  
	    ➝ делает код трудночитаемым, но не идеальная защита.
	### 3. Продвинутый уровень

	- ✅ **Cython** — перевод `.py` в C-код и сборка в `.so/.pyd`.  
	    ➝ выглядит как нативная библиотека, разобрать сложнее.
    
	- ✅ **Переписать критичные куски на C/C++ и подключить как расширение**.  
	    ➝ высокая защита, но дольше разработка.
    
	### 4. Максимальная защита

	- ✅ **SaaS / API-подход** — заказчик вообще не получает код, работает через API.  
	    ➝ стопроцентная защита от воровства, но требует серверной инфраструктуры.    
	- ✅ **Лицензионные механизмы** — запуск приложения только при наличии ключа/токена.  
	    ➝ контроль использования, но сложнее в поддержке.- В Python код компилируется сам в .pyc и легко восстанавливается. Нужно обернуть в упаковку PyInstaller или компиляцию в бинарь/ С-модули через Cython.
		- Проблемы:
				- **Дизассемблирование**: `.pyc` легко превращается обратно в читаемый код.
			- **Размер бинаря**: PyInstaller может собрать «монстра» на сотни мегабайт.
			- **Кроссплатформенность**: собранный бинарь под Linux не пойдёт на Windows.
			- **Отладка у заказчика**: если полностью скрыть код, заказчику может быть сложно интегрировать и отлаживать.

- Что такое [[GIL]]? для чего он нужен и какие проблемы вызывает?

Каким образом происходит общение между процессами?
	- Библиотека value для расшаривания общей памяти. очереди, каналы pipe, менеджерами. 