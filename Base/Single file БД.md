**Single-file база данных** — это СУБД, которая хранит _все данные полностью в одном файле_ на диске.  
Она не использует каталог файлов (как RocksDB, ClickHouse и др.), а имеет **единственный файл**, содержащий:

- таблицы
    
- индексы
    
- метаданные
    
- транзакционные журналы
    
- служебную информацию
    

Это делает такие БД очень удобными и лёгкими.

Ниже — подробное объяснение, примеры, устройства и сценарии.

---

# 🧩 Что такое single-file database?

Это база данных, где **всё хранилище — один файл**, например:

`mydata.sqlite local.db storage.realm cache.lmdb`

Такие БД — это _встраиваемые системы (embedded)_, работающие обычно как библиотека внутри приложения.

---

# 🧠 Особенности single-file БД

### ✔ Хранит всё в одном физическом файле

Упрощает:

- перенос
    
- резервное копирование
    
- тестирование
    
- миграции
    
- деплой приложений
    

### ✔ Не требует сервера

Работает в одном процессе:

- меньше накладных расходов
    
- высокая скорость
    
- нет сетевой задержки
    

### ✔ ACID-транзакции

Большинство single-file БД обеспечивают надёжность (WAL, MVCC).

---

# 🔥 Преимущества single-file СУБД

## ✔ Простота использования

Один файл → одна база → никакой настройки.

## ✔ Отлично подходит для встраиваемых приложений

Мобильные, desktop, IoT.

## ✔ Очень быстрый локальный доступ

Нет сетевых запросов, всё внутри процесса.

## ✔ Минимум внешних зависимостей

Приложение работает где угодно.

## ✔ Лёгкое бэкапы и переносимость

Можно просто скопировать файл.

---

# ⚠ Недостатки

### ❌ Несколько процессов не могут работать одновременно

Обычно разрешён один writer (например, SQLite).

### ❌ Не подходит для кластеров и распределённых систем

Нет репликации, шардирования (или они ограничены).

### ❌ Ограничения по размерам и параллельности

Обычно до сотен гигабайт, но максимум — локальная машина.

---

# 🏆 Популярные single-file БД

Вот самые известные.

---

## ⭐ 1. **SQLite**

Самая популярная single-file БД в мире.

- один файл хранения
    
- SQL-движок
    
- ACID через WAL
    
- используется в Android, iOS, Chrome, Firefox, Git, системах навигации
    

### Пример файла:

`appdata.sqlite`

---

## ⭐ 2. **RealmDB**

Документная single-file база.

- много используется в мобильной разработке
    
- намного быстрее SQLite в ряде сценариев
    
- объектная модель, без SQL
    

Файл:

`default.realm`

---

## ⭐ 3. **LMDB** (Lightning Memory-Mapped DB)

Очень быстрая key-value БД с copy-on-write.

- один файл данных (.mdb) + lock-файл
    
- супербыстрая
    
- ACID
    
- используется в OpenLDAP, приложениях требующих стабильности
    

---

## ⭐ 4. **LiteDB** (для .NET)

Библиотека на C#, полностью single-file.

- JSON-документная модель
    
- аналог MongoDB, но в одном файле
    
- используется в Blazor, desktop-приложениях
    

---

## ⭐ 5. **DuckDB**

“SQLite для аналитики” — колоночная OLAP СУБД в одном файле.

- идеальна для data science
    
- работает внутри Python, R, C++
    
- превосходит Pandas по скорости
    

Файл:

`analytics.db`

---

## ⭐ 6. **H2 / HSQLDB** (Java)

Может работать в режиме single-file.

---

# 🧱 Как они устроены внутри?

## 1. **Memory-mapped files (mmap)**

Доступ к файлу как к памяти → очень быстро.

Используют: LMDB, DuckDB.

## 2. **Copy-on-write**

Новая запись не перезаписывает старую → гарантирует атомарность.

## 3. **WAL (write-ahead log)**

Журнал, который защищает от сбоев.

SQLite использует WAL-файл:

`db.sqlite-wal`

## 4. **Страничная архитектура**

Файл разделён на страницы фиксированного размера (обычно 4 KB).

## 5. **B-tree или LSM-tree структуры**

SQLite — B-Tree  
DuckDB — columnar  
LMDB — B+Tree (CoW)

---

# 🧪 Где single-file базы используются

### ✔ Мобильные приложения

SQLite, Realm

### ✔ Desktop программы

Браузеры, IDE, почтовые клиенты

### ✔ IoT устройства

Хранение данных на ограниченных платформах

### ✔ Edge computing

Когда нет доступа к серверной БД

### ✔ Встроенные сервисы в микросервисах

Например: кэш, конфигурации, локальное KV-хранилище

### ✔ Data science

DuckDB → быстрые локальные аналитические запросы

---

# 🎯 Итог

**Single-file базы данных — это лёгкие, быстрые и автономные СУБД, встраиваемые в приложение и хранящие все данные в одном файле.**

Они дают:

- простоту распределения,
    
- высокую скорость,
    
- ACID,
    
- надёжность,
    
- автономность.
    

Лучшие представители:

- **SQLite**
    
- **DuckDB**
    
- **Realm**
    
- **LMDB**
    
- **LiteDB**

**Применение**:

**Пример**:

**Доп. ссылки:** [[Классы баз данных]]

**Tags**: #IT #Backend  #БазаДанных 