**GraphQL** — это язык запросов к API (создан Facebook в 2012, открыт в 2015),  
который позволяет **клиенту самому выбирать, какие данные получить от сервера.**

> 📖 Проще говоря:  
> **вместо десятков разных REST-эндпоинтов** ты обращаешься **к одному**,  
> и говоришь: _«дай мне вот эти поля вот у этого объекта»._

## Как GraphQL работает внутри

1. 📥 **Клиент** отправляет **запрос** (query) с указанием, какие поля нужны.
    
2. 🧠 **Сервер GraphQL** читает этот запрос и **сам решает**, какие данные достать из БД или других API.
    
3. 📤 **Возвращает JSON**, который содержит **только запрошенные поля**.
    

---

## 🧩 Основные типы операций в GraphQL

|Операция|Назначение|Аналог в REST|
|---|---|---|
|**Query**|Получить данные|GET|
|**Mutation**|Изменить данные (создать, обновить, удалить)|POST / PUT / DELETE|
|**Subscription**|Получать данные в реальном времени (через WebSocket)|WebSocket / SSE|

Плюсы:

| Преимущество                        | Что даёт                                                                |
| ----------------------------------- | ----------------------------------------------------------------------- |
| 🎯 **Ровно нужные данные**          | Клиент сам выбирает поля (никакого over/underfetching)                  |
| ⚡ **Один запрос вместо нескольких** | Уменьшает latency                                                       |
| 🧱 **Типизация схемы**              | Схема описана чётко, автогенерируется документация                      |
| 🔄 **Единая точка входа**           | Один endpoint `/graphql` вместо десятков REST                           |
| 🧩 **Гибкость**                     | Можно объединять данные из разных источников (БД, API, микросервисы)    |
| 🔍 **Инструменты разработчика**     | GraphiQL / Apollo / Playground — тестирование запросов прямо в браузере |

|Минус|Почему|
|---|---|
|⚙️ **Сложнее бэкенд**|Сервер GraphQL нужно спроектировать: схема, резолверы, типы|
|🚀 **Не всегда быстрее REST**|Если запросы тяжёлые и вложенные — может быть медленнее|
|🧩 **Кэширование сложнее**|Нет простого URL для GET-запросов|
|🔒 **Безопасность**|Клиент может запросить слишком много данных (нужен query depth limit)|
|📊 **Неудобно для простых API**|REST проще, если нужно просто CRUD|

**Применение**:

**Пример**:

**Доп. ссылки:** [[API]]

**Tags**: #Backend #IT #АрхитектуаПриложения 