Инструмент когда мы передаем некоторые данные из коллекции в функцию. Не пасспаковывая ее заранее.

**Пример**:
```
def sum_of_tree(a,b,c):
	return a+b+c 

tree = [1,2,3]
print(sum_of_three(*tree))
```


**Доп. ссылки:** [[*args **kwargs]]

**Tags**: #Python  #IT 