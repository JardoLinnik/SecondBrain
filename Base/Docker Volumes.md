Хранилище для данных которые контейнеры могут использовать - локальная база данных.

- Настройка
	- Указываем в Dockerfile Volume и путь к нему: `./app/data`
	- указываем в Makefile команду run c параметром  `-v name:/app/data`
- Работа с volumes в реальном времени.
	- дополнительно пишем в run `-v "/path_to_project:/app" -v /app/modules

**Проблема**:

**Пример**:

**Доп. ссылки:** [[*Docker]] [[Docker Образ]] [[Docker Контейнер]]

**Tags**: #Backend  #IT  #ОС #Сервер 