
Это база данных, где **основное хранилище — оперативная память**.  
Данные могут быть:

- _только в RAM_ (volatile)
    
- в RAM + периодически сохраняться на диск (semi-persistent)
    

Простой принцип:

> Скорость работы RAM используется для ускорения запросов, кэша, вычислений и доступа к данным.

---

# 🏎 Зачем нужны in-memory базы?

Они используются там, где важна **максимальная скорость**, например:

- обработка миллионов запросов в секунду
    
- low-latency сервисы
    
- realtime аналитика
    
- высоконагрузочные API
    
- хранение временных данных
    
- профили пользователей
    
- очереди и pub/sub
    

---

# 🧱 Как они работают внутри

### 1. **Хранение данных только в RAM**

Это даёт экстремально быстрый доступ:

- O(1) операции поиска в структурах типа hash map
    
- минимальная задержка (наносекунды)
    

### 2. **Записи не блокируют диск**

Нет медленного IO → нет узких мест.

### 3. **Специальные структуры данных**

Используются высокоэффективные структуры:

- Hash table
    
- Skip-list
    
- Compacted tree
    
- Bitmap
    
- HyperLogLog
    
- Bloom filter
    

Например, Redis использует:

- хеш-таблицы
    
- Ziplist
    
- SDS строки
    
- SkipList для сортированных множеств
    

### 4. **Опциональная персистентность**

Некоторые in-memory БД могут сохранять данные на диск, но всегда работают _из памяти_.




# Как защитить данные:

- Переодически записывать данные на диск в состоянии БД
- Записывать на диск журналы изменений
- Репликация состояния оперативной памяти на другие машины.

---

# 🔥 Плюсы in-memory баз данных

### ✔ Максимальная скорость

В 100–1000 раз быстрее дисковых БД.

### ✔ Низкая задержка (latency)

Идеально для:

- realtime систем
    
- финансовых операций
    
- игровых серверов
    

### ✔ Простые операции O(1)

Хеш-таблицы позволяют мгновенно находить данные.

### ✔ Идеальны для кэшей и сессий

Сессии пользователей, токены авторизации → идеальный кейс.

### ✔ Поддержка pub/sub

Сообщения доставляются за миллисекунды.

---

# ❌ Минусы in-memory БД

### ❌ 1. Дорого хранить большие объёмы

RAM ограничена и стоит дороже диска.

### ❌ 2. Возможна потеря данных

Если БД не настроена на персистентность, данные исчезнут при:

- отключении питания
    
- перезапуске сервиса
    
- падении ноды
    

### ❌ 3. Не всегда подходят для транзакций

Хотя некоторые (SAP HANA) обеспечивают ACID, большинство нет.

### ❌ 4. Ограничена размером памяти

Сложно хранить большие объемы логов или аналитики.

---

# 🏗 Типы in-memory баз

## 1. **Volatile (без сохранения на диск)**

Используются как кэш.

Примеры:

- Memcached
    
- Redis без RDB/AOF
    

---

## 2. **In-memory + persistence (гибрид)**

Данные хранятся в памяти, но периодически сбрасываются на диск.

Примеры:

- Redis (RDB, AOF)
    
- Aerospike (in-memory index + persisted data)
    
- Tarantool
    

---

## 3. **In-memory SQL базы**

Полноценные ACID БД, работающие в RAM.

Примеры:

- SAP HANA
    
- VoltDB
    
- MemSQL (SingleStore — гибрид)
    

---

# 🧪 Примеры популярных in-memory БД

## ⭐ Redis

Самая популярная in-memory база.  
Поддерживает:

- строки
    
- хеши
    
- списки
    
- множества
    
- sorted sets
    
- pub/sub
    
- битовые операции
    
- streams
    

Есть RDB и AOF persistence.

---

## ⭐ Memcached

Простой быстрый кэш.  
Только key-value, без персистентности.

---

## ⭐ Tarantool (от Mail.ru)

In-memory + встроенный Lua runtime  
Используется во ВКонтакте и больших нагруженных системах.

---

## ⭐ Aerospike

In-memory indexes  
Используется в рекламе, real-time bidding, low-latency сервисах.

---

## ⭐ SingleStore (бывш. MemSQL)

Выполняет SQL, работает в RAM, но поддерживает диск.

---

# 🧰 Где in-memory базы используются в реальности

### ✔ High-performance caching

CDN, API gateway, веб-сервисы.

### ✔ Сессии пользователей

Схемы авторизации (JWT, токены).

### ✔ Лимитирование запросов (rate limiting)

Redis incr → мгновенный доступ.

### ✔ Очереди, pub/sub, streaming

Redis Streams, Kafka-like сценарии.

### ✔ Realtime аналитика

Кликстримы, метрики.

### ✔ Хранение профилей пользователей

Низкая задержка для персонализации.

### ✔ Игровые серверы

Хранение состояния игры в RAM.







---

# 🧠 Итог

**In-memory базы данных — это сверхбыстрые хранилища, использующие оперативную память для максимальной производительности.**  
Они идеально подходят для high-load, кэшей, очередей, low-latency сервисов и realtime аналитики.

Но:

- ограничены объёмом памяти
    
- дороже дисковых
    
- могут терять данные без persistence

**Применение**:

**Пример**:

**Доп. ссылки:** [[Классы баз данных]]

**Tags**: #IT #Backend  #БазаДанных 