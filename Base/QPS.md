
**QPS (Queries Per Second)** — это

> 📊 **количество запросов (queries)**, обрабатываемых системой **за одну секунду**.

💡 Обычно этот термин применяют к **базам данных** и **поисковым системам**,  
где запросы = **SQL-запросы**, **поисковые запросы** или **вызовы к хранилищу данных**

## Где используют QPS

| Где                                                        | Что считается                                 |
| ---------------------------------------------------------- | --------------------------------------------- |
| 🧱 **Базы данных (SQL, NoSQL)**                            | Кол-во SELECT / INSERT / UPDATE в секунду     |
| 🔍 **Поисковые системы (Elasticsearch, Solr, OpenSearch)** | Кол-во поисковых запросов в секунду           |
| ☁️ **Кэши (Redis, Memcached)**                             | Кол-во операций GET/SET в секунду             |
| ⚙️ **Веб-сервисы**                                         | Иногда QPS = RPS (если запросы идут напрямую) |
## Где смотреть QPS

| Инструмент                     | Как показывает QPS                                                    |
| ------------------------------ | --------------------------------------------------------------------- |
| **Prometheus + Grafana**       | Метрика `mysql_global_status_questions` / `mongodb_op_counters_total` |
| **MySQL / PostgreSQL**         | `SHOW STATUS LIKE 'Queries';` + `SHOW GLOBAL STATUS`                  |
| **MongoDB**                    | `db.serverStatus().opcounters`                                        |
| **Redis**                      | `INFO stats` → `instantaneous_ops_per_sec`                            |
| **Elasticsearch / OpenSearch** | `_nodes/stats` → `search.query_total`                                 |

## Как увеличить QPS

| Метод                                  | Что делает                              |
| -------------------------------------- | --------------------------------------- |
| 🧱 Кэширование (Redis, CDN)            | Уменьшает количество обращений к БД     |
| ⚙️ Индексы в БД                        | Ускоряют поиск и выборки                |
| 🧩 Разделение (sharding, partitioning) | Распределяет нагрузку по узлам          |
| 🔄 Репликация                          | Чтение с реплик увеличивает QPS         |
| 💾 Connection Pooling                  | Сокращает время на установку соединений |
| 🧠 Оптимизация запросов                | Убирает лишние JOIN'ы, COUNT'ы и т.д.   |


**Применение**:

**Пример**:

Один HTTP-запрос от клиента:

`GET /user/1/orders`

Может вызывать внутри:

- 1 SQL-запрос к таблице `users`,
    
- 1 SQL-запрос к `orders`,
    
- 1 SQL-запрос к `products`.
    

📈 Итого:

> 1 RPS → 3 QPS

Потому что **1 HTTP-запрос вызывает 3 запроса к БД.**

**Доп. ссылки:** [[Base/Мониторинг]]

**Tags**: #Backend #IT #АрхитектуаПриложения 