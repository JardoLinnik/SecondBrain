**etcd** — это распределённое, строго согласованное key-value хранилище, построенное на протоколе консенсуса **Raft**.

Основная идея:

> маленькое, очень надёжное, предсказуемое хранилище для метаданных и конфигураций в распределённых системах.

Используется в:

- Kubernetes (центральное хранилище всех данных)
    
- CoreOS
    
- сервиса-дискавери
    
- распределённых блокировках
    
- системах управления конфигурацией
    

Легковесен, написан на Go, доступен через HTTP/gRPC.

---

# 🏗 Архитектура etcd-кластера

Обычно кластер состоит из:

- **3 узлов** (минимум для отказоустойчивости)
    
- иногда 5 или 7 (для более строгого SLA)
    

Roles:

- Все узлы равны (peer)
    
- Один назначается **Leader**
    
- Остальные — **Followers**
    

Все операции **записи** идут через лидера.  
Чтения могут быть локальными или согласованными.

---

# 🔐 Протокол консенсуса Raft (внутри etcd)

etcd использует **Raft**, состоящий из нескольких ключевых механизмов:

### 1️⃣ Лидер принимает команды (записи)

Клиент отправляет команду → лидер добавляет её в свой лог.

### 2️⃣ Репликация лога

Лидер транслирует запись всем follower-ам.

### 3️⃣ Подтверждение кворума

Большинство узлов должны подтвердить запись (кворум = N/2+1).

### 4️⃣ Коммит

Запись считается окончательно зафиксированной.

Преимущества Raft:

- простота реализации
    
- предсказуемость поведения
    
- компактность кода
    
- устойчивость к split-brain
    

---

# 📦 Модель хранения данных

etcd хранит данные как key-value пары.  
Ключи — бинарные строки, используются как деревья (псевдо-иерархия):

Пример:

`/configs/db/host /configs/db/port /services/api/instance1 /services/api/instance2`

Фактически дерево хранится как отсортированный список ключей.

---

# ⏳ Версионирование и транзакции

Каждая запись имеет:

- **Revision** (глобальная версия)
    
- **ModRevision** (номер изменения ключа)
    
- **CreateRevision**
    

Это позволяет:

### ✔ создавать watch

### ✔ делать транзакции

### ✔ обеспечивать history lookup

### ✔ реализовывать atomic compare-and-swap

---

# 🔭 Watch — одно из самых сильных мест etcd

Клиенты могут подписаться на изменения в ключах или префиксах.

Используется для:

- автоматической конфигурации
    
- сервис-дискавери
    
- реактивных систем
    
- обновления стейта в Kubernetes
    

Watch события отправляются через известный revision, что исключает гонки.

---

# 🔒 Распределённые блокировки в etcd

Используются:

- уникальные lease
    
- TTL
    
- атомарные операции (CAS)
    

Принцип:

1. Клиент создаёт lease (арендный ключ с TTL)
    
2. Привязывает к нему ключ блокировки
    
3. Если ключ создан — лок получен
    
4. Если lease истёк — лок автоматически освобождается
    

Благодаря этому etcd очень популярен для:

- распределённых cron-job
    
- координации сервисов
    
- failover систем
    

---

# ☸ Почему etcd = сердце Kubernetes

Kubernetes хранит _всю_ информацию в etcd:

- информация о pod-ах
    
- deployment-ах
    
- nodes
    
- secrets
    
- конфигурации
    
- статус контроллеров
    
- network allocation и т.д.
    

Почему etcd идеально подходит:

- строгая согласованность (важна для API Server)
    
- транзакции (например, создание pod-а)
    
- быстрый watch (контроллеры K8S реагируют на изменения)
    
- лёгкая репликация
    
- безопасность
    

В kube-api-server используется режим **quorum read/writes**, чтобы состояние было консистентным.

---

# 🌐 API и взаимодействие

etcd имеет два API:

### 1. **gRPC v3 API** (основной)

Используется в Kubernetes.

Операции:

- Put
    
- Get
    
- Delete
    
- Txn (транзакции)
    
- Lease
    
- Watch
    

### 2. HTTP v2 API (устаревший)

Совместимость сохранена для старых клиентов.

---

# 📌 Производительность

etcd оптимизирован под:

- малый размер данных
    
- множество чтений
    
- разумное количество записей
    
- большой объём watch-событий
    

**Не подходит** для хранения сотен мегабайт или больших blob.

---

# ⚙ Сильные стороны etcd

- 🔒 **сильная согласованность (CP в CAP)**
    
- ⚡ **быстрый watch**
    
- 🎯 **атомарные транзакции**
    
- 💡 **Raft — простой, надёжный**
    
- ☸ **глубокая интеграция с Kubernetes**
    
- 📦 **простой интерфейс**
    
- 🌐 **gRPC и HTTP API**
    

---

# 🛑 Слабые стороны etcd

- не подходит для больших данных (только метаданные)
    
- write throughput ограничен скоростью Raft
    
- чувствителен к latency между узлами
    
- требует ровного и быстрого SSD
    
- нужно контролировать размер WAL и снапшотов
    

---

# 🧪 Где используется в реальности

**Kubernetes** → всё состояние кластеров  
**CoreOS / Flatcar Linux** → конфигурации машин  
**Calico / Flannel** → сетевые метаданные  
**Docker Swarm (ранние версии)** → metadata store  
**OpenStack** → сервис-дискавери  
**Самописные микросервисы** → locks + configs + discovery

**Применение**:

**Пример**:

**Доп. ссылки:** [[Нереляционная база данных]]

**Tags**: #IT #Backend  #БазаДанных 