**Разряженный индекс** — это индекс, в котором **хранятся не все ключи таблицы**, а только часть из них.  
Он указывает на **страницы данных**, а не на каждую конкретную строку.

То есть индекс "разрежен" — содержит лишь **одну запись на блок (страницу)** данных или даже реже.

Объясняю максимально понятно 👇

---

# 🧩 Простое объяснение разряженного индекса

Представь, что таблица хранится так:

`Страница 1: id 1,2,3,4,5 Страница 2: id 6,7,8,9,10 Страница 3: id 11,12,13... ...`

**Плотный (dense) индекс** содержит запись на _каждый ключ_:

`1 → page1,row1 2 → page1,row2 3 → page1,row3 ...`

**Разряженный (sparse) индекс** содержит запись только **на первую строку каждой страницы**:

`1  → page1 6  → page2 11 → page3 ...`

Когда база ищет значение 8:

1. По индексу понимает: «8 находится между 6 и 11 → значит в page2».
    
2. Загружает page2 и уже внутри смотрит точную строку.
    

---

# 🎯 Зачем нужен sparse index?

Чтобы экономить память и место.

Если таблица огромная, то:

- плотный индекс будет **очень большим**
    
- разряженный индекс будет **намного меньше**
    

Поиск всё равно остаётся очень быстрым:

- O(log n) по индексу
    
- затем сканирование одной страницы (обычно 50–300 строк)
    

---

# ⚡ Преимущества разряженного индекса

### ✔ ⛳ Экономит место

Индекс может быть в 10–50 раз меньше плотного.

### ✔ ⚡ Быстрее загружается в память

Больше индекса помещается в RAM → быстрее запросы.

### ✔ 📦 Меньше I/O

Страниц для чтения меньше.

### ✔ 👍 Идеален для диапазонных запросов (`BETWEEN`, `>`, `>=`)

---

# ⚠ Недостатки разряженного индекса

### ❌ Поиск немного медленнее

Требуется дополнительно просканировать страницу.

Но страница — это 4–8К, то есть максимум пару сотен строк → дешево.

### ❌ Подходит только для **упорядоченных данных**

Разряженные индексы работают, если таблица отсортирована по ключу.

### ❌ Плохо для частых UPDATE, если нарушается порядок строк

(в некоторых системах требует реорганизации)

---

# 🗂 Где используется разряженный индекс в реальных СУБД?

## 🟦 **PostgreSQL**

Использует разряженные индексы в механизме **BRIN** (Block Range Index).

BRIN хранит:

- минимум
    
- максимум
    
- статистику по блоку
    

Это даже "супер-разряженный" индекс.

## 🟧 **MySQL / InnoDB**

InnoDB использует **кластеризованный B-Tree**, где вторичные индексы действуют как разряженные по отношению к кластеризованным страницам.

## 🟩 **HBase / Cassandra**

Используют sparse index по SSTable — запись только на boundary keys.

## 🟥 **Аналитические OLAP системы**

ClickHouse, Parquet, ORC — все используют **sparse skip indexes**:

- min/max на блок
    
- bloom фильтры
    
- inverted min-max индекс
    

## 🟪 **B-Tree индексы в классике**

В B-Tree внутренние узлы — это разряженный индекс, а листья — плотный.

---

# 🧠 Плотный vs разряженный индекс (коротко)

|Тип индекса|Что хранит|Размер|Скорость|
|---|---|---|---|
|**Плотный (dense)**|запись на каждую строку|большой|быстрый|
|**Разряженный (sparse)**|одну запись на страницу|маленький|чуть медленнее|

---

# 🧾 Когда разряженный индекс — идеальный выбор?

✔ большие таблицы (миллионы–миллиарды строк)  
✔ диапазонные запросы (`>` `<`, `BETWEEN`)  
✔ аналитика (OLAP)  
✔ столбцы, по которым редко обновляют данные  
✔ когда хочется экономить место

---

# 🎯 Итог

**Разряженный индекс (sparse index)** — это индекс, который хранит _не каждую строку_, а только одну запись на группу строк (обычно страницу).

Он:

- экономит место,
    
- быстро работает,
    
- идеально подходит для больших таблиц и диапазонных запросов,
    
- является основой BRIN, skip index, SSTable indexing.
- 

**Применение**:

**Пример**:

**Доп. ссылки:** [[Индексы]]

**Tags**: #IT #Backend  #БазаДанных 