Честно, самая тяжёлая задача в Т-Банке была та, где у нас поехал real-time движок остатков.  
Я прям помню момент, когда **нагрузка выросла**, Kafka начала ловить огромный lag, у нас события шли в неправильном порядке, Postgres начал ловить блокировки, а антифрод получал устаревшие данные и срабатывал неправильно.

И вот ты сидишь, смотришь в графики, а они только хуже становятся.  
Ты понимаешь, что система “дышит через раз”, а где конкретно ломается — непонятно.  
Это тот момент, когда мозг реально закипает:  
**с одной стороны — миллионы событий, с другой — всё взаимозависимо.**

Я начал разбираться по частям: сначала нашли проблему в ключах партиционирования Kafka — одна партиция перегружена,

Мы начали ковыряться глубже. Сначала выяснилось, что часть нагрузки съедала **одна-единственная партиция Kafka**, потому что ключ был выбран неудачно. События приходили пачками в одну точку, а остальные партиции простаивали.  
И ты смотришь и понимаешь: да, вот оно — но просто поменять ключ нельзя, иначе всё поведение сломается.

Параллельно в Postgres случалось **мясо**:  
`UPDATE` на горячих строках создавал цепочки блокировок, и ты видишь, как p95 растёт буквально на глазах. Всё взаимосвязано: Kafka даёт задержку → в базу летят bursts → база захлёбывается → антифрод получает устаревшие остатки → срабатывает неправильно → летят жалобы.

Это был момент, когда я реально сидел ночью и думал: _«Окей, а как это разрулить так, чтобы не развалить весь контур?»_

Но постепенно пазл сложился:

- мы перенесли ключи Kafka на `sku+location`,
    
- ввели **идемпотентность** обработки,
    
- заменили тяжелые `UPDATE` на `UPSERT`,
    
- вынесли историю в отдельную таблицу с партициями,
    
- добавили Redis как быстрый слой, чтобы антифрод не ждал базу.
    

После этого графики реально “выдохнули”:  
lag стал падать, блокировок почти не осталось, антифрод вернулся к нормальной работе.

И честно — это был один из тех кейсов, когда ты заканчиваешь задачу и думаешь:  
**«Вот теперь я реально понимаю, как работает highload в проде не только по книжкам».**



## Что именно происходило

1. **Redis стал дропать горячие ключи**, когда память подошла к пределу.  
    Из-за этого часть признаков (фичей) внезапно исчезала и восстанавливалась уже с задержкой.
    
2. **Модель получала половину нужного контекста.**  
    Представь: у пользователя есть история операций → это важный флаг.  
    Но кэш очищается, и модель видит будто _у пользователя “пустое прошлое”_.  
    В результате она думает:  
    **«Аномально! Такой паттерн похож на мошенника».**
    
3. **Kafka downstream сервисы начали отставать**, потому что на отдельных ключах нагрузка была очень неравномерная.
    
4. **Метрики при этом были зелёные**, потому что latency API оставалась нормальной, ошибок не было, но качество решений упало.
    

Это был один из тех кейсов, когда **всё технически работает, но логически — ломается».

---

## 😵 Что было сложным лично для меня

Самое неприятное — отсутствие явного “красного флага”.  
В логах — чисто.  
В трейсах — всё нормально.  
Просто модель принимает странные решения.

И ты сидишь, перебираешь варианты:

- ошибка в коде?
    
- сломан фичестор?
    
- неправильная версия модели?
    
- неправильные данные?
    
- просела Kafka?
    
- проблема в Redis?
    
- или это вообще поведение пользователей?
    

Это ощущается как **детектив на проде**, и от этого реально потеют ладони.

---

## 🛠 Как решили (коротко, но по сути)

1. **Включили xray-трейсы через OpenTelemetry** — стали видеть, какие именно фичи приходят в модель.
    
2. Быстро обнаружили, что у части пользователей фичи пустые.
    
3. Проверили Redis → увидели рост evictions.
    
4. Перенесли фичи на:
    
    - отдельный Redis-кластер,
        
    - с фиксированным TTL,
        
    - и ограничением по memory policy (noeviction).
        
5. Добавили fallback-логику:  
    если фича отсутствует → тянем свежие данные из Postgres.
    
6. Добавили алерт: «If eviction > 0 → сразу в on-call».
    

---

## ✔ Результат

- Модель стабилизировалась буквально в течение часа.
    
- Ложные срабатывания исчезли.
    
- Антифрод-цепочка вернулась в норму.
    
- А мы получили отличный опыт: **кэш может быть “зелёным”, но ломать бизнес-логику».
    

---

## 🗣 Как это сказать на собеседовании

> Один из самых сложных кейсов был, когда антифрод внезапно начал массово выдавать ложные срабатывания. Метрики были зелёные, ошибок не было, но решения модели стали странными.  
> В итоге выяснили, что Redis начал дропать горячие фичи, и модель получала неполный контекст.  
> Мы вынесли фичи в отдельный Redis-кластер, добавили fallback к Postgres и ввели алёрты на evictions. После этого качество решений вернулось, а цепочка антифрода стабилизировалась.