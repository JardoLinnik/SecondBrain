> **Apache Kafka** — это распределённая система потоковой передачи сообщений (streaming platform),  
> предназначенная для **высокопроизводительной, надёжной и масштабируемой доставки данных** между сервисами.
> 
> В отличие от классических брокеров (типа RabbitMQ), Kafka хранит сообщения **в виде лога** (append-only),  
> а не удаляет их после обработки, что позволяет:
> 
> - читать историю сообщений заново,
>     
> - подключать новых потребителей “с начала”,
>     
> - строить аналитические и событийные пайплайны.
>     
> 
> В Т-Банке Kafka используется как основа **event-driven архитектуры** — для потоков транзакций, скорингов, логов, метрик и интеграции микросервисов.

---

## 🔹 Плюсы Kafka

### ✅ 1. **Очень высокая производительность**

- Kafka — _high-throughput система_: миллионы сообщений в секунду.
    
- Оптимизирована под **пакетную запись** и **последовательный I/O** на диск.
    
- Использует **Zero Copy** и **page cache** → минимизирует накладные расходы.
    

📊 _Типичные цифры:_

> 1–10 млн сообщений/сек на кластере из 3–5 брокеров.

🧠 _В Т-Банке_: Kafka обрабатывает поток клиентских операций и событий скоринга — десятки миллионов событий в день.

---

### ✅ 2. **Хранение истории (лог)**

Kafka хранит сообщения в **топиках**, которые состоят из **партиций (partition)**.  
Сообщения не удаляются сразу после обработки, а **живут до retention-времени** (например, 7 дней или 1 месяц).

Это значит, что:

- можно **перечитывать историю**;
    
- новые подписчики могут “догонять” данные с нужного места;
    
- можно “реплеить” события при изменении логики.
    

🧠 _В Т-Банке_: при обновлении скоринговой модели можно перечитать исторические события (`replay`) и пересчитать результаты без потери данных.

---

### ✅ 3. **Горизонтальная масштабируемость**

Kafka масштабируется линейно:

- топики делятся на **партиции**;
    
- партиции распределяются по брокерам;
    
- консьюмеры объединяются в **consumer groups**.
    

💡 Это даёт масштабирование по throughput и по обработчикам.

---

### ✅ 4. **Надёжность и отказоустойчивость**

- Kafka хранит данные на диск (commit log).
    
- Репликация между брокерами (`replication.factor=3`).
    
- Автоматическое восстановление после сбоя.
    
- Поддержка **acks=all** — запись подтверждается, когда все реплики синхронизировались.
    

---

### ✅ 5. **Гарантии доставки**

Kafka поддерживает:

- **at most once** — без подтверждения;
    
- **at least once** — по умолчанию;
    
- **exactly once** — с transactional producer/consumer.
    

🧠 _В Т-Банке_: для событий транзакций и платежей используется `exactly once`, чтобы не задвоить операции.

---

### ✅ 6. **Интеграция и экосистема**

Kafka — не просто очередь, а целая платформа:

- **Kafka Streams** — потоковая обработка данных (Java API).
    
- **Kafka Connect** — коннекторы для БД, Elasticsearch, S3 и т.д.
    
- **ksqlDB** — SQL поверх потоков.
    
- **Schema Registry** — контроль версий Avro/Protobuf схем.
    

🧠 _В Т-Банке_: через Kafka Connect синхронизируются события между PostgreSQL, Redis и аналитическим DWH (ClickHouse).

---

## 🔻 Минусы Kafka

### ❌ 1. **Сложность администрирования**

Kafka — **непростая система**:

- нужен ZooKeeper (или KRaft в новых версиях);
    
- нужно настраивать партиции, репликацию, retention, offsets;
    
- требует мониторинга и DevOps-компетенций.
    

📉 Если нет опыта эксплуатации — можно “поймать” лаги, рассинхрон offsets, перегрузку брокеров.

---

### ❌ 2. **Не realtime, а near-realtime**

Kafka — _batched system_: сообщения отправляются и читаются пакетами.  
Это даёт высокую производительность, но latency обычно **5–200 мс**, а не миллисекунды, как в Redis или RabbitMQ.

🧠 Для push-уведомлений, где нужна <10 мс, Kafka — не лучший выбор.

---

### ❌ 3. **Нет сложной маршрутизации**

В отличие от RabbitMQ, Kafka не умеет гибко маршрутизировать сообщения.  
Только по **топику и ключу** → внутри топика порядок сохраняется только **в рамках одной партиции**.

📍 Если нужна сложная логика “если это → в ту очередь, если то → в другую”,  
лучше использовать RabbitMQ.

---

### ❌ 4. **Огромный объём данных**

Kafka хранит всё, и это может занимать терабайты.  
Нужно продумывать retention-политику, компрессию, очистку старых сообщений.

---

### ❌ 5. **“Тяжёлый” для простых задач**

Если задача — просто “отправить письмо в фоне” или “обработать push”, Kafka будет избыточна.  
Поднять кластер Kafka ради пары очередей — из пушки по воробьям.

📍 В таких кейсах лучше RabbitMQ / SQS.

---

## 📊 Kafka vs RabbitMQ (сводка)

|Параметр|**Kafka**|**RabbitMQ**|
|---|---|---|
|Модель|Лог (stream)|Очередь (queue)|
|Хранение|Сохраняет историю (retention)|Удаляет после ack|
|Скорость|Очень высокая (до млн msg/s)|Средняя (до 100k msg/s)|
|Латентность|5–200 мс|<10 мс|
|Масштабирование|Простое, горизонтальное|Сложное (кластер, HA)|
|Маршрутизация|Простая (по ключу)|Сложная (exchange, routing)|
|Использование|Event streaming, аналитика, интеграции|Очереди задач, фоновые джобы|
|Историчность|Есть|Нет|

---

## 🧩 Пример из Т-Банка

> В Т-Банке Kafka — это **ядро событийной архитектуры**.
> 
> - Все микросервисы публикуют бизнес-события: `transaction_created`, `client_updated`, `loan_approved`.
>     
> - Эти события подхватывают подписчики: скоринг, аналитика, antifraud, нотификации.
>     
> - Kafka обеспечивает гарантированную доставку, репликацию и возможность “replay” при ошибках.
>     
> - Для внутренних интеграций Kafka используется с **Schema Registry** (Avro/Protobuf).
>     
> - Для фоновых задач (email, push, отчёты) — **RabbitMQ**, так как там важна не история, а надёжная очередь.
>     

---

## 💬 Как красиво завершить ответ

> В целом, Kafka — это не просто очередь, а **система событийного лога**,  
> идеально подходящая для **event-driven архитектуры**, **аналитических пайплайнов** и **интеграции микросервисов**.
> 
> Её плюсы — масштаб, производительность и надёжность;  
> минусы — сложность и ресурсоёмкость.
> 
> В Т-Банке мы используем Kafka для событий и стриминга данных,  
> а RabbitMQ — для фоновых задач и ретраев,  
> таким образом балансируя **скорость, консистентность и надёжность.**