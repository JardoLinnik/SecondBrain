> **RabbitMQ** — это брокер сообщений, ориентированный на очереди (message queue) и надёжную доставку.
> 
> Он отлично подходит для **асинхронных задач**, **распределённых систем** и **интеграции микросервисов**, где важны гарантии доставки и гибкая маршрутизация.
> 
> Из плюсов — надёжность, простота интеграции, гибкость маршрутизации и поддержка многих протоколов.  
> Из минусов — сложная горизонтальная масштабируемость, падение производительности при большом количестве сообщений и относительно высокая латентность по сравнению с Kafka.
> 
> В Т-Банке мы используем RabbitMQ для фоновых задач, ретраев и очередей пушей, где нужна гарантированная доставка и контроль обработки.

---

## 🔹 Плюсы RabbitMQ

### ✅ 1. **Надёжная доставка**

- Поддерживает **подтверждения (ack/nack)**, повторную доставку и `dead-letter` очереди.
    
- Можно добиться **"at least once"** гарантии доставки.
    
- Есть **persistent messages** (сообщения не теряются при перезапуске брокера).
    

`# Пример: подтверждение сообщения channel.basic_ack(delivery_tag=method.delivery_tag)`

📍 _В Т-Банке_: используется для гарантированной доставки уведомлений и транзакций, где потеря недопустима.

---

### ✅ 2. **Гибкая маршрутизация**

RabbitMQ поддерживает **разные exchange-типы**:

- `direct` — по ключу маршрутизации (точно в очередь);
    
- `fanout` — broadcast во все очереди;
    
- `topic` — по шаблону (`user.*`, `*.created`);
    
- `headers` — по заголовкам сообщения.
    

📍 Это даёт мощную гибкость: можно строить сложные схемы доставки без кода.

---

### ✅ 3. **Поддержка разных протоколов**

Работает с **AMQP, MQTT, STOMP, HTTP API** — подходит и для backend, и для IoT/вебов.

---

### ✅ 4. **Простота интеграции**

Быстро поднимается, легко интегрируется с Python (pika, Celery), Java, Go, Node.js.  
Есть готовые SDK и клиенты.

📍 _В Т-Банке_: Celery с RabbitMQ используется для асинхронных задач — отчёты, уведомления, отложенные операции.

---

### ✅ 5. **Механизмы контроля нагрузки**

- `Prefetch count` — ограничивает количество необработанных сообщений на consumer.
    
- `Priority queues` — приоритетная обработка.
    
- `TTL` — время жизни сообщения.
    
- `Dead-letter exchange` — переотправка неуспешных сообщений.
    

📍 Удобно для построения retry-механизмов и “умных” очередей.

---

### ✅ 6. **Поддержка кластеров и HA**

- Есть **кластеризация** и **mirrored queues (HA queues)** для отказоустойчивости.
    
- Поддерживает **federation / sharding**, если нужно связать несколько RabbitMQ кластеров.
    

---

## 🔻 Минусы RabbitMQ

### ❌ 1. **Сложное горизонтальное масштабирование**

RabbitMQ — **не распределённый лог**, как Kafka.  
Сообщения физически хранятся в очередях конкретных нод, и при росте нагрузки:

- возникает неравномерное распределение нагрузки;
    
- миграция сообщений между нодами может быть дорогой.
    

📉 Масштабировать RabbitMQ можно, но это требует хорошего тюнинга (sharding / federation).

---

### ❌ 2. **Падение производительности при большом количестве сообщений**

RabbitMQ не рассчитан на миллионы сообщений в секунду.  
После ~50–100k сообщений/сек начинают расти задержки и использование памяти.  
Особенно если много очередей или маленькие сообщения.

📍 _В Т-Банке_: для real-time потоков и событий (миллионы событий/сек) используем **Kafka**, а RabbitMQ — только для фоновых задач и ретраев.

---

### ❌ 3. **Накладные расходы при подтверждениях (ack/nack)**

Если включить `durable + persistent + ack` на каждое сообщение,  
RabbitMQ делает запись на диск → это безопасно, но замедляет производительность.

---

### ❌ 4. **Сложность при больших топологиях**

Сложные маршруты (topic → fanout → DLQ → retry) становятся трудно управляемыми,  
особенно при большом числе exchange’ов и binding’ов.

---

### ❌ 5. **Не хранит историю событий**

RabbitMQ — это **очередь**, а не лог.  
Сообщение удаляется после обработки.  
Если тебе нужно “дочитывать историю” или подключать новые консьюмеры с начала — нужен Kafka-подход.

---

## 📊 RabbitMQ vs Kafka (кратко)

|Характеристика|RabbitMQ|Kafka|
|---|---|---|
|Модель|Очередь (queue)|Поток (log)|
|Маршрутизация|Exchange / routing keys|Topic / partition|
|Историчность|Сообщения удаляются|Сообщения сохраняются|
|Производительность|До ~100k msg/s|Миллионы msg/s|
|Латентность|Низкая|Чуть выше, но стабильная|
|Надёжность|Ack/nack, DLQ|Offset-коммиты|
|Use-case|Очереди задач, retry, уведомления|Потоки событий, логирование, аналитика|

---

## 🧩 Пример из Т-Банка

> В Т-Банке RabbitMQ используется как **сервисный брокер задач**:
> 
> - для фоновых джобов (отправка push/email/смс);
>     
> - для retry-процессинга неудачных операций;
>     
> - для очередей отчётов (batch job processing);
>     
> - для антирейтовых лимитов, где важен порядок обработки.
>     
> 
> Kafka, наоборот, используется для стриминга и событий: клиентские действия, скоринг, аудиты.
> 
> Такое разделение даёт и надёжность, и масштаб.

---

## 💬 Как красиво закончить ответ

> В целом, RabbitMQ — это надёжный брокер с богатой функциональностью маршрутизации и гарантированной доставкой.
> 
> Он отлично подходит для **асинхронных задач и микросервисных очередей**,  
> но не предназначен для **high-throughput стриминга**.
> 
> В Т-Банке мы используем его там, где важна **гарантированная доставка и контроль retry-процессинга**,  
> а Kafka — там, где важна **масштабируемость и история событий**.