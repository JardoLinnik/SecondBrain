> **Очередь** — это структура данных, работающая по принципу **FIFO (First In — First Out)**, то есть _первым пришёл — первым вышел_.
> 
> Представь обычную очередь в магазине: кто пришёл раньше, тот обслуживается раньше.

---

## 🔹 Основные операции

|Операция|Что делает|Аналогия|
|---|---|---|
|`enqueue`|добавить элемент в конец очереди|встать в очередь|
|`dequeue`|удалить элемент из начала очереди|выйти первым|
|`peek` (или `front`)|посмотреть первый элемент, не удаляя|посмотреть, кто первый в очереди|
|`isEmpty`|проверить, пуста ли очередь||

---

## 🔹 Пример на Python
```
`from collections import deque 
 queue = deque()
   queue.append('A')     
   # enqueue queue.append('B')
    print(queue.popleft())
     # dequeue -> 'A' print(queue)    
             # deque(['B'])`

```
➡️ Здесь используется `collections.deque`, потому что обычный `list` в Python неэффективен для операций удаления с начала (`O(n)`), а `deque` делает это за `O(1)`.

---

## 🔹 Временная сложность операций

|Операция|Сложность|
|---|---|
|enqueue|O(1)|
|dequeue|O(1)|
|peek|O(1)|

---

## 🔹 Применение на практике

1. **Буферизация и обработка потоков данных**  
    Например, в сетевых программах, принтерах или очередях задач — элементы поступают и обрабатываются по порядку.
    
2. **Очередь задач (Task Queue)**  
    Планировщики ОС и серверы (например, RabbitMQ, Celery, Kafka) используют очереди для управления задачами: кто первым поступил — тот первым обрабатывается.
    
3. **Обход графа в ширину (BFS)**  
    Для обхода графа по уровням используется очередь:
    
    `from collections import deque def bfs(graph, start):     visited = {start}     queue = deque([start])     while queue:         node = queue.popleft()         for neighbor in graph[node]:             if neighbor not in visited:                 visited.add(neighbor)                 queue.append(neighbor)`
    
4. **Симуляции и системы обслуживания**  
    Моделирование работы банков, серверов, касс — всё, где клиенты обрабатываются по порядку.
    
5. **Асинхронная обработка**  
    Очереди применяются в веб-серверах, микросервисах и распределённых системах для балансировки нагрузки.
    

---

## 🔹 Виды очередей

|Вид|Описание|
|---|---|
|**Обычная очередь (FIFO)**|стандартное поведение|
|**Двунаправленная очередь (deque)**|добавление и удаление с обеих сторон|
|**Очередь с приоритетом (Priority Queue)**|элемент с наивысшим приоритетом выходит первым|
|**Кольцевая очередь (Circular Queue)**|используется в ограниченных буферах (например, аудио)|

---

## 💬 Как можно красиво завершить ответ:

> В целом, очередь — это структура, которая идеально подходит для **управления последовательной обработкой событий или задач**.  
> Она обеспечивает предсказуемый порядок, что важно во многих системах — от алгоритмов обхода графов до очередей сообщений в распределённых приложениях