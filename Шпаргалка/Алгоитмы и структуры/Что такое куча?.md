> **Куча (heap)** — это **специальная структура данных в виде двоичного дерева**, которая удовлетворяет свойству **кучи**:
> 
> - в **мин-куче** каждый родитель меньше (или равен) своих потомков,
>     
> - в **макс-куче** каждый родитель больше (или равен) своих потомков.
>     
> 
> Таким образом, **корень** всегда содержит **минимальный (или максимальный)** элемент.

---

## 🔹 Ключевые свойства

1. **Полное бинарное дерево** — все уровни заполнены, кроме, возможно, последнего (и он заполняется слева направо).
    
2. **Быстрый доступ к минимуму/максимуму** — элемент всегда в корне.
    
3. **Эффективные операции вставки и удаления** — за `O(log n)`.
    

---

## 🔹 Основные операции и их сложность

|Операция|Описание|Сложность|
|---|---|---|
|`insert(x)`|добавить элемент в кучу|`O(log n)`|
|`get_min()` / `get_max()`|вернуть корень|`O(1)`|
|`extract_min()` / `extract_max()`|удалить корень и восстановить свойство кучи|`O(log n)`|
|`heapify()`|построить кучу из массива|`O(n)`|

---

## 🔹 Пример на Python

В Python есть стандартный модуль **`heapq`**, который реализует **мин-кучу**:

`import heapq  nums = [5, 3, 8, 1] heapq.heapify(nums)      # превращает список в кучу  heapq.heappush(nums, 2)  # добавить элемент print(heapq.heappop(nums))  # удалить и вернуть минимальный элемент → 1 print(nums)  # оставшаяся куча`

📌 Чтобы получить **макс-кучу**, часто просто хранят отрицательные значения:

`heapq.heappush(nums, -value)`

---

## 🔹 Визуальный пример (мин-куча)

```
        1
      /   \
     3     8
    / \
   5   4

```

Здесь `1` — корень, минимальный элемент.  
Если мы удалим `1`, структура "перестраивается", чтобы следующий минимальный оказался на вершине.

---

## 🔹 Где используется куча

1. **Очередь с приоритетом (Priority Queue)**  
    — позволяет быстро получать элемент с наивысшим приоритетом.  
    Например, планировщик задач, маршрутизация, симуляции.
    
2. **Сортировка кучей (Heap Sort)**  
    — алгоритм сортировки на основе кучи: `O(n log n)` по времени, `O(1)` по памяти.
    
3. **Нахождение k-го наименьшего/наибольшего элемента**
```
import heapq
nums = [7, 4, 6, 3, 9, 1]
k = 3
print(heapq.nlargest(k, nums)[-1])  # 3-й по величине элемент

```
    
4. **Алгоритмы на графах**
    
    - **Дейкстра**, **A*** — используют кучу для выбора вершины с минимальным расстоянием.
        
    - **Прим** — для построения минимального остовного дерева.
        
5. **Слияние отсортированных списков** (`merge k sorted lists`).
    

---

## 🔹 Отличие от других структур

|Структура|Доступ к элементу|Сложность вставки|Сложность удаления|
|---|---|---|---|
|Массив|По индексу — O(1)|O(1)|O(n)|
|Сбалансированное дерево|По значению — O(log n)|O(log n)|O(log n)|
|**Куча**|Только min/max — O(1)|O(log n)|O(log n)|

---

## 💬 Как можно красиво завершить ответ:

> В целом, куча — это оптимальная структура для ситуаций, где нужно **часто получать минимальный или максимальный элемент**, не сортируя весь массив.
> 
> На практике она лежит в основе **очередей с приоритетом**, **алгоритмов на графах** и **эффективной сортировки**.