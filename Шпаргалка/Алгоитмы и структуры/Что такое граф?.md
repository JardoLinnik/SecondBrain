> **Граф** — это структура данных, состоящая из **вершин (узлов)** и **рёбер (связей)** между ними.
> 
> Формально:  
> Граф `G = (V, E)`, где
> 
> - `V` — множество **вершин (vertices)**,
>     
> - `E` — множество **рёбер (edges)**, соединяющих пары вершин.
>     

В целом, граф — это универсальная модель связей.  
Он используется практически везде, где есть **отношения между объектами**:  
люди, города, устройства, зависимости, маршруты.  
Умение работать с графами — основа для понимания многих алгоритмов и структур данных.


---

## 🔹 Виды графов

|Тип графа|Описание|Пример|
|---|---|---|
|**Ориентированный (directed)**|Рёбра имеют направление (стрелки)|социальная сеть “кто на кого подписан”|
|**Неориентированный (undirected)**|Рёбра без направления|дружба, дороги|
|**Взвешенный (weighted)**|У рёбер есть “вес” (например, расстояние, стоимость)|карта маршрутов|
|**Невзвешенный (unweighted)**|Все рёбра равнозначны|простая связь “есть/нет”|
|**Связный**|Между любыми вершинами есть путь|единая сеть|
|**Ацикличный (DAG)**|Нет циклов (петель)|зависимости между задачами|
|**Мультиграф**|Между вершинами могут быть несколько рёбер|транспортные маршруты|

---

## 🔹 Как граф представляют в коде

1. **Списки смежности (adjacency list)** — эффективный способ:
    
    `graph = {     'A': ['B', 'C'],     'B': ['A', 'D'],     'C': ['A', 'D'],     'D': ['B', 'C'] }`
    
2. **Матрица смежности (adjacency matrix)** — таблица `n×n`, где `1` или вес обозначает наличие ребра:
    
        `A B C D   A 0 1 1 0   B 1 0 0 1   C 1 0 0 1   D 0 1 1 0`
    

---

## 🔹 Пример визуально

   `A —— B    |    |    C —— D`

➡️ Вершины: A, B, C, D  
➡️ Рёбра: (A-B), (A-C), (B-D), (C-D)

---

## 🔹 Где применяются графы на практике

1. **Социальные сети** — пользователи (вершины), связи “дружбы” или “подписок” (рёбра).
    
2. **Карты и навигация** — города как вершины, дороги как рёбра с весами.
    
3. **Поиск путей** — алгоритмы Дейкстры, A*, BFS.
    
4. **Планирование задач и зависимостей** — ориентированные ацикличные графы (DAG), например, в системах сборки или CI/CD.
    
5. **Сети и маршрутизация** — анализ соединений между компьютерами.
    
6. **Рекомендательные системы** — связи между пользователями и товарами.
    
7. **Анализ связей в данных** — графовые базы данных (Neo4j, ArangoDB).
    

---

## 🔹 Алгоритмы для работы с графами

|Цель|Алгоритм|Сложность|
|---|---|---|
|Поиск в ширину|BFS|O(V + E)|
|Поиск в глубину|DFS|O(V + E)|
|Кратчайший путь|Дейкстра|O(E log V)|
|Минимальное остовное дерево|Прим, Краскал|O(E log V)|
|Топологическая сортировка|Kahn / DFS|O(V + E)|
|Поиск компонент связности|DFS / BFS|O(V + E)|

---

## 🔹 Пример BFS (поиск в ширину)

```
from collections import deque

def bfs(graph, start):
    visited = set()
    queue = deque([start])
    while queue:
        node = queue.popleft()
        if node not in visited:
            print(node)
            visited.add(node)
            queue.extend(graph[node])

```