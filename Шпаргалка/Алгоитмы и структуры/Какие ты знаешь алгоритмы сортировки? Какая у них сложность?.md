## **Основные алгоритмы сортировки и их сложности**

|Алгоритм|Идея|Средняя сложность|Худшая сложность|Доп. память|Стабильность|Комментарий|
|---|---|---|---|---|---|---|
|**Пузырьковая (Bubble Sort)**|Попарно меняем элементы, “всплывает” максимум|O(n²)|O(n²)|O(1)|✅ Да|Простая, учебная|
|**Сортировка вставками (Insertion Sort)**|Вставляем каждый элемент в уже отсортированную часть|O(n²)|O(n²)|O(1)|✅ Да|Хороша для маленьких массивов или почти отсортированных|
|**Сортировка выбором (Selection Sort)**|Находим минимальный элемент и ставим в начало|O(n²)|O(n²)|O(1)|❌ Нет|Минимум обменов, но медленная|
|**Быстрая сортировка (Quick Sort)**|Разделяем по “опорному” элементу (разделяй и властвуй)|O(n log n)|O(n²)|O(log n)|❌ Нет|Самая быстрая на практике (если выбрать хороший pivot)|
|**Сортировка слиянием (Merge Sort)**|Делим пополам и сливаем отсортированные части|O(n log n)|O(n log n)|O(n)|✅ Да|Стабильная, используется в `Timsort`|
|**Пирамидальная (Heap Sort)**|Использует структуру кучи (heap)|O(n log n)|O(n log n)|O(1)|❌ Нет|Надёжная, но часто медленнее QuickSort|
|**Timsort (Python, Java)**|Гибрид Merge Sort и Insertion Sort|O(n log n)|O(n log n)|O(n)|✅ Да|Реализован в Python (`sorted`, `.sort()`)|
|**Сортировка подсчётом (Counting Sort)**|Считаем количество вхождений значений|O(n + k)|O(n + k)|O(k)|✅ Да|Только для целых чисел ограниченного диапазона|
|**Поразрядная сортировка (Radix Sort)**|Сортируем по разрядам (по цифрам)|O(d·n)|O(d·n)|O(n + k)|✅ Да|Быстрая для чисел и строк|
|**Bucket Sort (по корзинам)**|Распределяем элементы по диапазонам|O(n + k)|O(n²)|O(n + k)|✅ Да|Эффективна для равномерных данных|

---

## 🔹 **Как я бы рассказал устно:**

> Я знаю несколько основных алгоритмов сортировки:
> 
> Простые — **пузырьковая**, **вставками**, **выбором** — они имеют сложность `O(n²)` и обычно используются только в обучении.
> 
> Более эффективные — **быстрая сортировка**, **слиянием** и **пирамидальная** — работают за `O(n log n)` и применяются на практике.
> 
> В Python, например, встроенная функция `sorted()` реализует **Timsort**, который сочетает идеи сортировки вставками и слиянием, обеспечивая `O(n log n)` в среднем и `O(n)` в почти отсортированных данных.
> 
> Есть также линейные алгоритмы (`O(n)`) — **Counting Sort**, **Radix Sort**, **Bucket Sort**, но они применимы только для ограниченных диапазонов чисел или специальных структур данных.

---

## 🔹 **Если интервьюер попросит сравнение:**

|Алгоритм|Когда использовать|
|---|---|
|**QuickSort**|Большие, случайные данные — быстрая на практике|
|**MergeSort**|Когда важна стабильность и предсказуемость|
|**HeapSort**|Когда нужна сортировка без рекурсии и доп. памяти|
|**InsertionSort**|Почти отсортированные массивы|
|**Counting/Radix**|Ограниченный диапазон чисел (например, [0..1000])|

---

## 🔹 **Пример: QuickSort (для наглядности)**

`def quicksort(arr):     if len(arr) <= 1:         return arr     pivot = arr[len(arr)//2]     left = [x for x in arr if x < pivot]     middle = [x for x in arr if x == pivot]     right = [x for x in arr if x > pivot]     return quicksort(left) + middle + quicksort(right)`

➡️ Средняя сложность: `O(n log n)`  
➡️ Худшая (если pivot выбран плохо): `O(n²)`

---

## 💬 **Как красиво завершить ответ:**

> В целом, я выбираю алгоритм сортировки исходя из **размера данных**, **требований к стабильности** и **ограничений по памяти**.
> 
> На практике чаще всего применяются **QuickSort**, **MergeSort**, и их гибриды вроде **Timsort**, потому что они дают наилучшее сочетание скорости и стабильности.

---