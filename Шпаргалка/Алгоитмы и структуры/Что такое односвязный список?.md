> **Односвязный список** — это структура данных, состоящая из **цепочки узлов (nodes)**,  
> где каждый узел хранит **значение** и **ссылку (указатель)** на следующий узел.
> 
> Последний элемент указывает на `None` (то есть “конец списка”).

---

## 🔹 Визуально

`[1 | • ] → [2 | • ] → [3 | • ] → [4 | None]`

Каждый элемент (`Node`) знает только о **следующем**.  
То есть можно двигаться **только вперёд**.

---

## 🔹 Структура узла и списка на Python

```
class Node:
    def __init__(self, value):
        self.value = value
        self.next = None

class LinkedList:
    def __init__(self):
        self.head = None

    def append(self, value):
        new_node = Node(value)
        if not self.head:
            self.head = new_node
            return
        current = self.head
        while current.next:
            current = current.next
        current.next = new_node

    def print_list(self):
        current = self.head
        while current:
            print(current.value, end=" -> ")
            current = current.next
        print("None")

```

**Пример использования:**

```
lst = LinkedList()
lst.append(10)
lst.append(20)
lst.append(30)
lst.print_list()
# 10 -> 20 -> 30 -> None

```

---

## 🔹 Основные операции и их сложность

|Операция|Описание|Сложность|
|---|---|---|
|**Добавление в начало**|вставка нового узла перед `head`|`O(1)`|
|**Добавление в конец**|нужно пройти весь список|`O(n)`|
|**Удаление элемента**|требует поиск предыдущего|`O(n)`|
|**Поиск элемента**|нужно пройти список|`O(n)`|
|**Доступ по индексу**|нельзя напрямую, только через итерацию|`O(n)`|

---

## 🔹 Преимущества

✅ Эффективное добавление/удаление в начале списка (`O(1)`)  
✅ Гибкий размер — не нужно заранее знать длину  
✅ Не требует непрерывного участка памяти (в отличие от массива)

---

## 🔹 Недостатки

❌ Нельзя быстро получить элемент по индексу (`O(n)`)  
❌ Увеличенные затраты памяти (каждый узел хранит указатель)  
❌ Неудобен для обратного прохода (нельзя идти “назад”)

---

## 🔹 Где используется

1. **Реализация стеков и очередей**
    
2. **Управление памятью и динамическими структурами**
    
3. **Undo/Redo, история действий**
    
4. **Хэш-таблицы (chaining)** — при разрешении коллизий
    
5. **Реализация буферов, потоков данных**
    

---

## 🔹 Сравнение с массивом

|Критерий|Массив|Односвязный список|
|---|---|---|
|Доступ по индексу|O(1)|O(n)|
|Вставка в начало|O(n)|O(1)|
|Вставка в конец|O(1)* (если есть размер)|O(n)|
|Размер|Фиксированный|Динамический|
|Использование памяти|Компактное|С указателями (больше памяти)|

---

## 💬 Как можно красиво завершить ответ:

> Односвязный список — это простая, но гибкая структура данных,  
> которая хорошо подходит, когда нужно часто добавлять или удалять элементы,  
> особенно в начале списка, и порядок доступа не критичен.
> 
> Он лежит в основе многих других структур — стеков, очередей и хэш-таблиц.