> **Бинарный поиск (binary search)** — это алгоритм для **поиска элемента в отсортированном массиве**,  
> который работает по принципу **“разделяй и властвуй”**.
> 
> Он многократно делит диапазон поиска пополам, пока не найдёт нужное значение  
> (или не убедится, что его нет).

---

## 🔹 Как работает бинарный поиск

1. Берём **средний элемент** массива.
    
2. Если он равен искомому — ✅ нашли.
    
3. Если искомое значение **меньше среднего** → продолжаем искать в **левой половине**.
    
4. Если **больше** → ищем в **правой половине**.
    
5. Повторяем, пока диапазон не сузится.
    

---

### 🔸 Пример

Ищем число `7` в массиве `[1, 3, 5, 7, 9, 11, 13]`

1. Средний элемент = `7`
    
2. Совпадает с искомым → нашли за 1 шаг ✅
    

Если бы искали `9`:

- шаг 1: середина `7` → 9 > 7 → идём вправо
    
- шаг 2: проверяем `[9, 11, 13]`, середина `11` → 9 < 11 → идём влево
    
- шаг 3: проверяем `9` → нашли ✅
    

---

## 🔹 Код на Python

```
def binary_search(arr, target):
    left, right = 0, len(arr) - 1
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1

```

---

## 🔹 Сложность

|Характеристика|Оценка|
|---|---|
|**Время (в среднем)**|`O(log n)`|
|**В худшем случае**|`O(log n)`|
|**Память (итеративная версия)**|`O(1)`|
|**Память (рекурсивная версия)**|`O(log n)`|

---

## 🔹 Когда применяется бинарный поиск

1. **Поиск элемента в отсортированном массиве**
    
2. **Поиск границы условия** — например, “первый элемент больше X”
    
3. **Поиск оптимального значения** в задачах “минимум/максимум при условии”  
    (например, подбор минимального времени, длины, температуры)
    
4. **Алгоритмы на числовых ответах** — бинарный поиск по ответу (“binary search on answer”)
    
5. **Поиск в словарях, индексах, базах данных** — B-деревья, бинарные индексы
    
6. **Игровая логика и вычислительная геометрия** — пересечения, поиск точки и т. д.
    

---

## 🔹 Примеры из практики

- Проверка, есть ли пользователь в отсортированном списке ID
    
- Поиск границы в логах (например, “первая ошибка после 10:00”)
    
- Алгоритмы вроде **Двоичного поиска по времени**, **по высоте**, **по цене** — для оптимизации
    

---

## 💬 Как красиво завершить ответ

> Бинарный поиск нужен, когда данные **отсортированы**,  
> и нужно **очень быстро находить элементы или границы условий**.
> 
> Его ключевое преимущество — **логарифмическая сложность O(log n)**,  
> что делает его **в десятки раз быстрее линейного поиска** на больших объёмах данных.