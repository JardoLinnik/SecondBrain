> **Бинарное дерево** — это **дерево**, в котором **каждый узел может иметь не более двух потомков**:  
> левый (`left child`) и правый (`right child`).

То есть у каждого узла максимум две ветви.

---

## 🔹 Структура

```
        A
       / \
      B   C
     / \
    D   E

```

- `A` — **корень**
    
- `B`, `C` — **дети** `A`
    
- `D`, `E` — **дети** `B`
    
- `D`, `E`, `C` — **листья**
    

---

## 🔹 Основные термины

|Термин|Значение|
|---|---|
|**Корень (root)**|вершина без родителя|
|**Лист (leaf)**|узел без детей|
|**Родитель / потомок**|связь между уровнями|
|**Высота дерева**|макс. количество уровней|
|**Поддерево**|часть дерева, тоже являющаяся деревом|

---

## 🔹 Виды бинарных деревьев

|Вид|Описание|
|---|---|
|**Полное бинарное дерево**|каждый уровень, кроме последнего, полностью заполнен|
|**Полное (Complete)**|все уровни заполнены, кроме, возможно, последнего (заполняется слева направо)|
|**Сбалансированное**|высота левого и правого поддеревьев отличается не более чем на 1|
|**Бинарное дерево поиска (BST)**|левый потомок < узел < правый потомок|
|**Куча (Heap)**|родитель ≥ (или ≤) потомков, не обязательно упорядочено по значению|

---

## 🔹 Пример узла и дерева на Python

```
class Node:
    def __init__(self, value):
        self.value = value
        self.left = None
        self.right = None

# Создаём дерево
root = Node(10)
root.left = Node(5)
root.right = Node(15)
root.left.left = Node(2)
root.left.right = Node(7)

```

---

## 🔹 Обходы бинарного дерева

|Обход|Порядок|Пример|Используется для|
|---|---|---|---|
|**Preorder**|Корень → Лево → Право|10, 5, 2, 7, 15|Копирование дерева|
|**Inorder**|Лево → Корень → Право|2, 5, 7, 10, 15|Получение отсортированных данных в BST|
|**Postorder**|Лево → Право → Корень|2, 7, 5, 15, 10|Удаление дерева|
|**Level order (BFS)**|По уровням|10, 5, 15, 2, 7|Используется в поисках и очередях|

---

## 🔹 Пример обхода Inorder

```
def inorder(node):
    if node:
        inorder(node.left)
        print(node.value, end=' ')
        inorder(node.right)

inorder(root)  # Выведет: 2 5 7 10 15

```

---

## 🔹 Где используется бинарное дерево

1. **Поиск и сортировка данных** → бинарные деревья поиска (BST).
    
2. **Базы данных и индексирование** → B- и AVL-деревья.
    
3. **Выражения и компиляторы** → синтаксические деревья (expression tree).
    
4. **Сжатие данных** → дерево Хаффмана.
    
5. **Иерархические структуры** → файловая система, DOM в браузере.
    

---

## 💬 Как можно завершить ответ:

> В целом, бинарное дерево — это основа многих алгоритмов.  
> Оно объединяет идею иерархии и эффективности:  
> поиск, вставка и удаление могут выполняться за `O(log n)` в сбалансированных деревьях,  
> поэтому оно активно используется в структурах вроде **BST**, **куч** и **AVL**.