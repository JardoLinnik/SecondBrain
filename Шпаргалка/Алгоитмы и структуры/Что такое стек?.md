
**Стек** — это структура данных, работающая по принципу **LIFO (Last In — First Out)**, то есть _последним пришёл — первым вышел_.

Представить его можно как стопку тарелок: последнюю, которую положили сверху, мы первой и снимем.

У стека есть два основных действия:

- `push` — добавить элемент на вершину стека,
- `pop` — удалить элемент с вершины и вернуть его.

Дополнительно часто есть:

- `peek` (или `top`) — посмотреть верхний элемент, не удаляя,
- `isEmpty` — проверить, пуст ли стек.

 На практике стек используется там, где нужно **запоминать последовательность действий**, а затем **восстанавливать их в обратном порядке**.  
> Это естественно следует из его принципа **LIFO (Last In — First Out)** — последним вошёл, первым вышел

**Где используется стек:**

- выполнение **рекурсии** (системный стек вызовов);
- **обратная польская нотация** и вычисление выражений;
- **парсинг скобок** и синтаксический анализ;
- **отмена действий (undo)** в приложениях;
- **обход графов (DFS)**.
    

---

📈 **Сложность операций:**

- `push` — O(1)
- `pop` — O(1)
- `peek` — O(1)


## 💡 Основные области применения стека

### 1. **Рекурсия и стек вызовов**

Каждая функция при вызове сохраняет в стеке:

- адрес возврата (куда вернуться после выполнения),
- локальные переменные,
- параметры функции.
- 
➡️ Именно поэтому при слишком глубокой рекурсии возникает **Stack Overflow** — стек переполняется.

**Пример:**

`def fact(n):     if n == 0:         return 1     return n * fact(n-1)`

Здесь каждый вызов `fact()` помещается в **стек вызовов** до тех пор, пока не дойдём до `n = 0`.

---

### 2. **Обратная польская нотация (Reverse Polish Notation, RPN)**

При вычислении выражений без скобок (например, `3 4 + 2 *`) используется стек:

- числа кладутся в стек,
    
- при встрече оператора (`+`, `*` и т. д.) два верхних числа извлекаются, вычисляются, и результат снова помещается в стек.
    

Это применяется при написании интерпретаторов и компиляторов.

---

### 3. **Проверка правильности скобок**

Одна из самых частых задач на собеседовании.

`def is_balanced(s):     stack = []     for ch in s:         if ch in "({[":             stack.append(ch)         elif ch in ")}]":             if not stack or "({[".index(stack.pop()) != ")}]".index(ch):                 return False     return not stack`

➡️ Стек помогает убедиться, что каждая открывающая скобка закрыта в правильном порядке.

---

### 4. **Отмена действий (Undo / Redo)**

Во многих приложениях (текстовые редакторы, Photoshop):

- все изменения помещаются в **стек действий**;
    
- операция Undo — это `pop` (удаляем последнее действие);
    
- для Redo можно использовать **второй стек**, куда кладутся отменённые шаги.
    

---

### 5. **Обход графов и деревьев**

В **обходе в глубину (DFS)** стек используется для запоминания пути:

- можно использовать рекурсию (внутренний стек вызовов),
    
- или явный стек (итеративная реализация).
    

`def dfs(graph, start):     stack = [start]     visited = set()     while stack:         node = stack.pop()         if node not in visited:             visited.add(node)             stack.extend(graph[node])`

---

### 6. **Парсеры и компиляторы**

При разборе выражений или кода стек используется для:

- хранения промежуточных операторов,
    
- отслеживания вложенности конструкций (`if`, `for`, `{}` и т. д.).
    

---

### 7. **Навигация в браузере**

Когда вы переходите по страницам:

- текущая страница кладётся в **стек “назад”**;
    
- нажатие “Назад” — `pop()` из этого стека;
    
- “Вперёд” — используется отдельный стек.
    

---

## ⚙️ Сводка

| Область          | Как используется стек             |
| ---------------- | --------------------------------- |
| Рекурсия         | Хранение вызовов функций          |
| Скобки / парсинг | Проверка корректности вложенности |
| Undo / Redo      | История действий                  |
| Навигация        | История переходов                 |
| DFS              | Запоминание пути обхода           |
| Компиляторы      | Построение синтаксического дерева |
| Вычисления       | Обратная польская нотация         |
