
# Движок

На проекте мультиканальной обработки товарных остатков я занимался **разработкой и оптимизацией backend-части движка**, который агрегирует и синхронизирует данные о товарах между разными источниками — склад, онлайн-витрина, партнёрские интеграции.
> 
> Моя зона ответственности включала:
> 
> 1. **Проектирование архитектуры** — разработка схемы обмена событиями через Kafka, настройка топиков и партиционирования по SKU, продумывание ключей идемпотентности.
>     
> 2. **Реализация сервисов** на **FastAPI** с асинхронным пайплайном обработки событий (Kafka → Redis → PostgreSQL).
>     
> 3. **Оптимизацию конкурентных обновлений** в БД — внедрил `versioned-upsert` (обновление только при новой версии данных), чтобы избежать гонок при записи остатков.
>     
> 4. **Интеграцию Redis** как слой агрегации и кэширования для снижения нагрузки на базу.
>     
> 5. **Наблюдаемость и мониторинг** — настроил метрики Prometheus (lag, latency, delta), алерты в Grafana, централизованные логи в Loki.
>     
> 6. **CI/CD и окружения** — подготовил Helm-чарты для деплоя в Kubernetes, настроил пайплайн с Kaniko и GitLab CI, добавил прогон тестов и линтеров (ruff, mypy, pre-commit).
>     
> 7. **Инцидент-менеджмент и постинциденты** — участвовал в расследованиях ошибок синхронизации, создавал runbook-и и шаблоны миграций без даунтайма.
>     

> Благодаря этим доработкам мы добились стабильной консистентности данных (<1 сек рассинхронизации между каналами), уменьшили нагрузку на базу на ~40%, и сделали систему устойчивой к всплескам и дубликатам событий.

---

## 💬 Короткий устный вариант (30–40 секунд)

> Я занимался backend-частью движка остатков — проектировал архитектуру, реализовывал обработку событий через Kafka и FastAPI,  
> оптимизировал запись в PostgreSQL через версионированные upsert-ы и Redis для агрегации.
> 
> Также отвечал за мониторинг и стабильность: метрики, алерты, логирование, CI/CD пайплайн.
> 
> В итоге удалось добиться консистентных данных между каналами и снизить нагрузку на базу почти вдвое.

---

# Антифрод

> В Т-Банке я участвовал в разработке **отдельного микросервиса антифрода**, который взаимодействовал с моделью машинного обучения, разработанной другой командой.
> 
> Наша команда отвечала за **продуктовую и техническую обёртку вокруг модели** — доставку данных, хранение результатов и интеграцию с другими системами банка.

### 🔹 Моя зона ответственности включала:

1. **Проектирование архитектуры сервиса.**  
    Я спроектировал асинхронный пайплайн на **FastAPI + Kafka**:
    
    - события транзакций приходят в Kafka,
        
    - сервис подтягивает необходимые данные (профиль клиента, историю транзакций),
        
    - формирует feature-пакет,
        
    - отправляет его в ML-сервис (через REST/gRPC),
        
    - и сохраняет результат проверки в PostgreSQL + Redis для быстрых запросов.
        
2. **Работа с производительностью и надёжностью.**
    
    - Добавил **асинхронные воркеры** для обработки Kafka-событий,
        
    - реализовал **идемпотентность** и **retry-механику** при ошибках ML-запроса,
        
    - ввёл **batch-обработку** для снижения нагрузки на БД,
        
    - внедрил **кэширование признаков** (Redis), чтобы не вызывать модель при дублирующихся событиях.
        
3. **Интеграция с моделью.**
    
    - Модель жила как отдельный сервис (у ML-команды), мы общались через REST/gRPC.
        
    - Я реализовал адаптер, который конвертирует внутренние данные в формат feature vector модели и обратно.
        
    - Добавил **версионирование моделей** — возможность переключаться между версиями модели без даунтайма через feature-флаг.
        
4. **Мониторинг и наблюдаемость.**
    
    - Подключил **Prometheus-метрики**: время ответа модели, success/error rate, Kafka lag, latency end-to-end.
        
    - Настроил **Sentry** и централизованные логи (Loki).
        
    - Добавил дашборды в Grafana для ML-команды — они могли отслеживать деградацию точности и задержку.
        
5. **Инцидент-менеджмент.**
    
    - Участвовал в разборе инцидента, когда задержка модели возросла → сделал fallback с кэша.
        
    - Добавил защиту от деградации: если ML-сервис не отвечает, антифрод отдаёт решение по “простым правилам” (rule-based backup).
        

---

## 🧩 Пример результатов

- Сервис стабильно обрабатывал **до 50k событий/мин** с latency < 300 мс.
    
- Благодаря кешированию и batch-записям нагрузка на БД снизилась на ~40%.
    
- Отказ ML-модуля теперь не влияет на основной поток транзакций.
    
- Вся команда получила прозрачные метрики и трейсинг end-to-end (от Kafka до PostgreSQL).
    

---

## 💬 Как можно сказать это устно (на 40–50 секунд)

> На антифроде я разрабатывал отдельный микросервис, который обрабатывал события транзакций и взаимодействовал с моделью, обучаемой другой командой.
> 
> Мы построили асинхронный пайплайн на FastAPI и Kafka: сервис собирал данные по клиенту, формировал feature-пакет, вызывал ML-модель и сохранял результат проверки.
> 
> Я отвечал за архитектуру, надёжность и производительность — реализовал идемпотентность, batch-обработку, кэширование признаков и fallback при сбоях модели.
> 
> В итоге сервис стал обрабатывать десятки тысяч событий в минуту с latency меньше 300 мс и устойчиво переживал отказ ML-сервиса.


# Система прогнозирования товарных остатков

Разрабатывал **систему прогнозирования товарных остатков**, которая помогает отделу закупок принимать решения по объёмам и срокам поставок.

Моей задачей было построить **потоковую архитектуру подготовки данных и обучение ML-модели** на исторических продажах, поставках и промо-акциях — при этом не нагружая основной контур, где работают онлайн-продажи и витрины.


1.> В системе прогнозирования товарных остатков я отвечал за **backend-часть и инфраструктуру вокруг ML-модели**.
> 
> Команда data science занималась обучением модели прогнозирования спроса, а моя задача как backend-разработчика — сделать так, чтобы модель могла работать в продакшене: получать актуальные данные, вызываться в нужные моменты, и публиковать результаты в бизнес-системы.

---

## 🔹 Конкретно, чем я занимался:

### 1. **Проектирование архитектуры данных**

- Спроектировал поток обработки данных из Kafka: события о продажах, поставках и возвратах агрегировались в real-time.
    
- Сервис на **FastAPI** валидировал и нормализовал данные, добавлял справочную информацию (SKU, склад, регион) и писал результат в staging PostgreSQL.
    
- Сформировал схему данных (ORM-модели, партиции, индексы) и обеспечил отказоустойчивость пайплайна.
    

---

### 2. **Формирование признаков и подготовка данных для модели**

- Реализовал **сервис feature-engineering**: по расписанию формировал агрегированные данные (скользящие средние продаж, интервалы поставок, коэффициенты сезонности и т.д.).
    
- Делал это пакетно — через SQL и Python (pandas, asyncpg), с сохранением результатов в Redis и PostgreSQL.
    
- Обеспечил автоматическое обновление данных по событиям из Kafka, без ручных ETL-запусков.
    

---

### 3. **Интеграция модели и оркестрация**

- Разработал **обёртку вокруг ML-модели**: сервис общался с моделью по REST/gRPC, отправлял подготовленные данные и принимал прогноз.
    
- Организовал **batch-инференс** — модель вызывается пакетно по расписанию или при обновлении данных.
    
- Реализовал механизм **fallback-а**: если модель недоступна, используется последний прогноз из кеша Redis.
    
- Добавил версионирование моделей (через фиче-флаги) — можно было переключать модель без релиза.
    

---

### 4. **Публикация результатов**

- После получения прогноза сервис записывал результат в PostgreSQL и публиковал событие в Kafka (`forecast_ready`).
    
- Другие системы (витрина запасов, отчётность, аналитика) подписывались на эти данные.
    
- Настроил выгрузку в BI-витрины и ClickHouse для аналитиков.
    

---

### 5. **Мониторинг, надёжность, CI/CD**

- Настроил Prometheus-метрики (время инференса, ошибки, свежесть данных, Kafka lag).
    
- Сделал Grafana-дашборды и алерты.
    
- Подключил Sentry и структурированные JSON-логи.
    
- Поддерживал CI/CD: сборка контейнера через Kaniko, тесты, деплой Helm-чартом в Kubernetes.
    

---

## 💬 Как можно сказать это устно (на 45–60 секунд)

> Я занимался backend-частью системы прогнозирования товарных остатков.  
> Моя зона ответственности — это обработка и подготовка данных, интеграция с моделью и доставка результатов в бизнес-витрины.
> 
> Мы собирали данные о продажах и поставках через Kafka, агрегировали их в FastAPI-сервисе и формировали признаки для модели.
> 
> Сама модель была у ML-команды, я реализовал обёртку для её вызова, кеширование, батч-обработку и публикацию прогнозов в Kafka и PostgreSQL.
> 
> Также отвечал за мониторинг, алерты и CI/CD пайплайн — чтобы модель стабильно работала в продакшене.
> 
> В итоге система обновляет прогноз каждые 15 минут, без влияния на основной контур, и данные автоматически попадают в витрину запасов.