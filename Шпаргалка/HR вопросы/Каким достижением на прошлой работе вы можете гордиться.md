> Одним из достижений, которым я действительно горжусь, было участие в разработке системы прогнозирования товарных остатков.
> 
> Мы построили сервис, который собирает данные о продажах и поставках в потоковом режиме, подготавливает признаки и взаимодействует с ML-моделью для прогноза спроса.
> 
> Я отвечал за архитектуру backend-части — сбор данных из Kafka, feature-engineering и интеграцию с моделью, а также за доставку результатов в витрины запасов.
> 
> В результате система обновляет прогноз каждые 15 минут без нагрузки на основной контур, снизила долю _out-of-stock_ примерно на 12% и помогла отделу закупок принимать более точные решения.
> 
> Мне особенно приятно, что это решение стало **частью ключевого бизнес-процесса**, а код и подход, который мы реализовали, потом взяли как шаблон для других data-driven сервисов.

---

## 🧩 Вариант 2 — Про антифрод-сервис (если хочешь сделать акцент на надёжности и архитектуре)

> На прошлом проекте я горжусь разработкой и стабилизацией антифрод-сервиса, который проверял транзакции в реальном времени.
> 
> Сервис взаимодействовал с моделью машинного обучения, обучаемой другой командой. Я спроектировал и реализовал backend — обработку Kafka-событий, формирование feature-пакетов, вызовы модели и сохранение результатов.
> 
> Главная сложность была в том, что при пиках нагрузки система начинала отставать и задерживать проверки. Я оптимизировал пайплайн: вынес запросы к ML в асинхронные задачи, добавил кэширование и batch-обработку, перераспределил партиции Kafka.
> 
> После этого latency проверки снизилось с 3 секунд до 180 мс, а lag Kafka стабилизировался почти до нуля.
> 
> Это решение позволило держать SLA на уровне 99.9% даже в пиковые периоды, и сейчас этот подход используется как стандарт для всех real-time сервисов в нашей команде.

---

## 🧩 Вариант 3 — Про движок мультиканальной обработки остатков (если хочешь показать системность и влияние)

> Я горжусь разработкой движка мультиканальной обработки товарных остатков.
> 
> Это распределённая система, которая синхронизирует данные между складами, витриной и партнёрскими каналами.
> 
> До этого данные часто расходились, и пользователи видели неверное наличие товара. Я спроектировал механизм идемпотентных обновлений и _versioned upsert_ в PostgreSQL, чтобы гарантировать консистентность при конкурентных обновлениях.
> 
> После внедрения система стала держать согласованность данных <1 секунды между всеми каналами, а нагрузка на базу снизилась почти на 40%.
> 
> Это решение повысило стабильность платформы и стало базой для последующих микросервисов обработки остатков.

---

## 💬 Короткий устный вариант (на 40 секунд)

> Самое значимое достижение — я разработал backend-часть системы прогнозирования товарных остатков.
> 
> Сервис собирает данные о продажах, формирует признаки и взаимодействует с ML-моделью для прогноза спроса.
> 
> Я отвечал за архитектуру, поток данных и интеграцию с моделью.  
> В итоге система обновляет прогноз каждые 15 минут, снизила долю out-of-stock примерно на 12% и стала ключевым элементом цепочки закупок.