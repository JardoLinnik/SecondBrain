> В команде мы используем **Ruff** как основной линтер и форматтер — он значительно ускоряет проверки по сравнению с классическими `flake8` и `pylint`.
> 
> Ruff у нас интегрирован в **pre-commit** и **GitLab CI/CD** — он проверяет стиль, типичные ошибки, сортирует импорты и даже умеет автофиксить часть замечаний.
> 
> Для форматирования мы оставили **Black**, потому что он полностью стандартизирует код.
> 
> То есть сейчас у нас связка:  
> **Ruff + Black + Mypy**, и это покрывает 99% сценариев линтинга и типизации.

---

## 🔹 Почему Ruff популярен (и почему его могли выбрать в Т-Банке)

1. ⚡ **Очень быстрый** — написан на Rust, работает в десятки раз быстрее, чем `flake8 + pylint`.
    
2. 🧩 **Комбинирует множество плагинов**:  
    Поддерживает правила `flake8`, `isort`, `pyupgrade`, `pylint` и др.
    
3. 🛠️ **Поддерживает автофикс** (`ruff check --fix`)
    
4. 🧠 **Простая интеграция с pre-commit и CI/CD**
    
5. 🔄 **Совместим с Black** — Ruff форматирует импорты, Black — сам код.
    

---

## 🔹 Пример конфига (из реальных пайплайнов)

```
# pyproject.toml
[tool.ruff]
line-length = 100
target-version = "py311"
select = ["E", "F", "I", "UP"]  # ошибки, формат, импорты, улучшения синтаксиса
ignore = ["E501"]  # длину строки контролирует black

[tool.ruff.isort]
known-first-party = ["app"]
combine-as-imports = true

[tool.black]
line-length = 100
target-version = ["py311"]

```

📘 **В CI:**

```
lint:
  stage: lint
  image: python:3.11
  script:
    - pip install ruff black mypy
    - ruff check .
    - black --check .
    - mypy .

```


📘 **В pre-commit:**

```
repos:
  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: v0.5.0
    hooks:
      - id: ruff
        args: [--fix]
  - repo: https://github.com/psf/black
    rev: 23.9.1
    hooks:
      - id: black
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.6.1
    hooks:
      - id: mypy


```


---

## 🔹 Как ты можешь привести это как реальный опыт

> Раньше мы использовали `flake8` и `isort`, но перешли на **Ruff**,  
> потому что он покрывает почти те же правила, работает быстрее и умеет автофиксить импорты и простые ошибки.
> 
> В GitLab CI он прогоняется на каждом merge request, а локально — через pre-commit.
> 
> Это ускорило пайплайн lint-stage почти в 5 раз и уменьшило количество мелких комментариев на ревью.

---

## 💬 Как можно завершить ответ

> Так что Ruff сейчас фактически заменил связку `flake8 + isort + pylint`,  
> и для крупных Python-команд — это уже стандарт.
> 
> Мы оставили Black для автоформатирования и Mypy для типизации — всё остальное делает Ruff.