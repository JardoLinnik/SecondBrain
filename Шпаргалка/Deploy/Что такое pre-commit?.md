> **pre-commit** — это инструмент, который запускает автоматические проверки кода **перед тем, как он попадёт в репозиторий**.
> 
> Мы используем его в команде в Т-Банке, чтобы гарантировать единый кодстайл и качество кода ещё **до пуша в GitLab**.
> 
> Например, перед каждым коммитом у нас автоматически запускаются **Ruff, Black, Mypy и проверки форматирования импортов**.
> 
> Если хоть один хук падает — коммит просто не проходит. Это экономит время на ревью и не даёт «грязному коду» попасть в CI.

---

## 🔹 Что делает pre-commit

- Автоматически запускает заданные проверки до `git commit`.
    
- Если хоть одна проверка не проходит, коммит **отклоняется**.
    
- Может не только проверять, но и **автоматически исправлять** (например, Black форматирует код).
    

---

## 🔹 Пример из твоего проекта (реально как в Т-Банке)

> В нашем репозитории FastAPI-сервисов pre-commit используется почти повсеместно.  
> Разработчик делает `git commit`, и локально запускаются хуки:
> 
> - `ruff` — линтер и импорт-органайзер,
>     
> - `black` — автоформатирование,
>     
> - `mypy` — статическая типизация,
>     
> - `trailing-whitespace`, `end-of-file-fixer` — чистка мусора.
>     
> 
> После установки pre-commit такие ошибки просто перестают доходить до ревью.

---

## 🔹 Пример `.pre-commit-config.yaml`

```
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.5.0
    hooks:
      - id: ruff
        args: [--fix]
  - repo: https://github.com/psf/black
    rev: 23.9.1
    hooks:
      - id: black
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.6.1
    hooks:
      - id: mypy
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      - id: end-of-file-fixer
      - id: trailing-whitespace

```

### Установка:

`pip install pre-commit pre-commit install`

После этого при каждом `git commit` автоматически запускаются все проверки.

---

## 🔹 Как это помогает в команде

- 🧼 **Гарантирует чистый код** — без мусора, без пропущенных импортов.
    
- 🚫 **Отсекает ошибки до CI/CD** — не тратится время на лишние пайплайны.
    
- 🧩 **Единый стиль кода у всей команды** — независимо от IDE.
    
- ⚡ **Повышает скорость ревью** — ревьюер не пишет “поставь кавычку”, а обсуждает логику.
    

📘 _В Т-Банке:_ pre-commit обязательный инструмент для всех dev-команд — он включён в dev-образ и шаблоны сервисов.  
Локально разработчики даже не задумываются об этом: просто коммитят — а pre-commit всё форматирует, линтит и проверяет.

---

## 💬 Как можно завершить ответ

> По сути, pre-commit — это **локальный “мини-CI”**, который гарантирует качество кода ещё до пуша.
> 
> Благодаря ему мы почти полностью убрали из ревью обсуждения стиля и типичных ошибок — пайплайны стали чище, а команда быстрее двигается.