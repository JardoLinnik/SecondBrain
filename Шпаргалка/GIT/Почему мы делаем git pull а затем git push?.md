> Мы делаем **`git pull` перед `git push`**,  
> чтобы **синхронизировать локальную ветку с удалённой** и избежать конфликтов.
> 
> `git pull` подтягивает все новые коммиты с сервера,  
> а `git push` — отправляет наши локальные изменения обратно в удалённый репозиторий.

---

## ⚙️ Что делает каждая команда

### 🡇 `git pull`

- Забирает изменения с удалённого репозитория (`origin`).
    
- Обновляет твою локальную ветку.
    
- Делает то же самое, что:
    
    `git fetch git merge origin/main`
    

🔹 Пример:

`git pull origin main`

> После этого у тебя локальный код совпадает с последним состоянием на сервере.

---

### 🡅 `git push`

- Отправляет твои **локальные коммиты** в удалённый репозиторий.
    
- То есть делает “публикацию” твоих изменений, чтобы их увидели другие.
    

🔹 Пример:

`git push origin main`

---

## 🔁 Почему нужно сначала `pull`, потом `push`

Представь, что ты и коллега работаете над одной веткой `main`:

- Он уже успел запушить свои изменения;
    
- А ты пытаешься запушить свои.
    

Если ты просто сделаешь `git push`, Git скажет:

`! [rejected] main -> main (fetch first)`

📢 То есть: “в удалённой ветке есть новые коммиты, которых у тебя нет”.

Поэтому нужно:

1. Сделать `git pull`, чтобы подтянуть его изменения.
    
2. Git объединит ваши коммиты (merge или rebase).
    
3. Потом `git push` — чтобы отправить уже актуальную историю на сервер.
    

---

## 💬 Как можно сказать это на собеседовании

> Мы сначала делаем `git pull`, чтобы обновить локальную ветку и убедиться, что у нас последняя версия кода.
> 
> Это помогает избежать конфликтов при отправке изменений.
> 
> После этого `git push` — чтобы загрузить наши собственные коммиты в удалённый репозиторий.
> 
> Иначе Git не позволит пушить, если история расходится.

---

## 🧠 Короткий ответ (на 20 секунд)

> `git pull` подтягивает новые изменения с сервера,  
> а `git push` — отправляет мои.
> 
> Поэтому сначала `pull`, чтобы синхронизировать код и избежать конфликтов,  
> потом `push`, чтобы отправить свои коммиты.