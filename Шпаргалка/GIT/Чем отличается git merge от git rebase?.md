> `git merge` и `git rebase` решают одну задачу — **объединение изменений из разных веток**,  
> но делают это по-разному.
> 
> **`merge`** создаёт **новый коммит слияния**, сохраняя историю обеих веток как есть,  
> а **`rebase`** переписывает историю — “перенося” коммиты поверх другой ветки, чтобы история выглядела линейно.
> 
> В Т-Банке мы обычно используем `rebase` при подготовке feature-веток перед merge request,  
> чтобы история была чистой и без merge-коммитов,  
> а `merge` — для основного объединения через GitLab, чтобы зафиксировать точку вливания в `develop` или `main`.

---

## 🔹 Пример на практике

Представь, есть две ветки:

```
main:    A --- B
feature:       \--- C --- D

```

### 🧩 `git merge main`

```
main:    A --- B ----------- M
feature:       \--- C --- D /

```
👉 Создаётся **merge-коммит (M)**, история сохраняется ветвистой.  
✅ Плюсы:

- сохраняет контекст разработки;
    
- безопасно (не меняет старые коммиты).  
    ⚠️ Минусы:
    
- история засоряется “Merge branch ‘main’...” коммитами;
    
- может быть сложнее читать `git log`.
    

---

### 🧩 `git rebase main`

```
main:    A --- B
feature:           \--- C' --- D'

```

👉 `feature` как бы “переписывается” поверх актуальной `main`.  
✅ Плюсы:

- история **линейная**, выглядит чисто;
    
- проще смотреть через `git log --oneline`.  
    ⚠️ Минусы:
    
- нельзя делать rebase общих веток (меняет хэши);
    
- требует аккуратности при конфликте.
    

---

## 🔹 Как это используется у нас (пример из Т-Банка)

> В Т-Банке у нас типичный **Git flow**:
> 
> - `main` — стабильная прод-ветка;
>     
> - `develop` — интеграционная;
>     
> - `feature/*` — задачи;
>     
> - `hotfix/*` — быстрые исправления.
>     
> 
> Перед созданием MR в GitLab я делаю:
> 
> `git fetch origin git rebase origin/develop`
> 
> — чтобы подтащить последние изменения и избежать merge-коммитов.
> 
> А уже при **вливании через MR** GitLab выполняет `merge --no-ff`, чтобы история сохраняла точку интеграции.

---

## 🔹 Пример команд

```
# Merge
git checkout feature/payments
git merge develop
# создаст merge-коммит

# Rebase
git checkout feature/payments
git rebase develop
# перепишет коммиты поверх develop

```
---

## 🔹 Пример реальной истории

### После merge:

```
*   9f2a1a1 (HEAD -> feature) Merge branch 'develop' into feature
|\
| * 83f4a4c fix metrics labels
| * e1f3c3b add kafka consumer
* | 6a77b2f update README
|/
* a1d5f2b initial commit

```
### После rebase:

```
* 83f4a4c fix metrics labels
* e1f3c3b add kafka consumer
* 6a77b2f update README
* a1d5f2b initial commit

```

---

## 💬 Как красиво закончить ответ

> Если коротко —  
> **`merge`** сохраняет историю как есть, а **`rebase`** делает её линейной.
> 
> В продакшене мы используем **`rebase` для чистоты истории в feature-ветках**,  
> и **`merge --no-ff` для интеграции в develop/main**,  
> чтобы видеть, какие фичи и когда были влиты.
> 
> Это помогает при отладке и расследованиях инцидентов — видно, какой MR внёс изменения в конкретный релиз.