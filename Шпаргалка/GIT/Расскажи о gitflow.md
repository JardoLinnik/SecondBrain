> **Git Flow** — это стратегия (workflow) работы с ветками в Git,  
> которая помогает команде **структурировать процесс разработки**,  
> разделить **фичи, релизы, хотфиксы** и **продакшн-код**.

Предложена была Винсентом Дриссеном (Vincent Driessen).  
Она определяет **чёткие правила**:  
какие ветки создаются, зачем и как они между собой взаимодействуют.

---

## 🧱 Основные ветки в Git Flow

|Ветка|Назначение|
|---|---|
|`main`|Продакшн-код. Только стабильные релизы.|
|`develop`|Основная ветка для разработки. Сюда вливаются все фичи.|
|`feature/*`|Разработка новых фич. После завершения вливается в `develop`.|
|`release/*`|Подготовка релиза: фиксы, документация, тесты. После — в `main` и `develop`.|
|`hotfix/*`|Срочные исправления продакшна. После — в `main` и `develop`.|

---

## ⚙️ Как это работает (пошагово)

### 1️⃣ Разработка новой фичи:

```
git checkout develop
git checkout -b feature/anti-fraud-logic
# ... пишем код ...
git commit -m "Добавил логику антифрода"
git checkout develop
git merge feature/anti-fraud-logic
git branch -d feature/anti-fraud-logic

```

### 2️⃣ Подготовка релиза:

```
git checkout develop
git checkout -b release/1.2.0
# ... тесты, фиксы ...
git checkout main
git merge release/1.2.0
git tag -a v1.2.0 -m "Release 1.2.0"
git checkout develop
git merge release/1.2.0
git branch -d release/1.2.0

```

### 3️⃣ Срочный хотфикс:

```
git checkout main
git checkout -b hotfix/1.2.1
# ... фикс ...
git checkout main
git merge hotfix/1.2.1
git tag -a v1.2.1 -m "Hotfix 1.2.1"
git checkout develop
git merge hotfix/1.2.1

```

---

## 💡 Почему Git Flow удобен

✅ Позволяет **разделять стабильный и активный код**  
✅ Упрощает **выпуск релизов и хотфиксов**  
✅ Легко понять, **в какой ветке что происходит**  
✅ Хорошо подходит для **долгих релизных циклов** (банки, enterprise)

---

## ⚠️ Но! Когда Git Flow — не лучший выбор

- Если вы релизитесь **каждый день / по CI/CD**,  
    Git Flow может быть **слишком тяжёлым**.
    
- В таких случаях чаще используют **Trunk Based Development** или **GitHub Flow**  
    — когда все работают от `main`, а фичи живут коротко.
    

---

## 💬 Как можно сказать на собеседовании

> Git Flow — это модель ветвления, где есть стабильная ветка `main` для продакшна и `develop` для интеграции новых фич.
> 
> Для новых задач создаются ветки `feature/*`, для подготовки релизов — `release/*`,  
> а для срочных фиксов — `hotfix/*`.
> 
> После завершения каждая ветка вливается в нужные основные, чтобы поддерживать чистую историю.
> 
> Мы использовали этот подход в команде, чтобы контролировать релизы и быстро откатываться при необходимости.