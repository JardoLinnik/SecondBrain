Мультиэтапная сборка, slim образы, объединение команд в один слой, исключать лишние файлы через .dockerignore, удалять лишние файлы (кэш и временные файлы) зависимостей в одном слое с установкой зависимостей.