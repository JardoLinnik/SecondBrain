> Существует несколько способов инвалидации кэша — то есть обновления или удаления устаревших данных, когда источник изменился.
> 
> Основные подходы — **по времени (TTL)**, **по событиям**, **по ключам (explicit delete)**, **через версии данных (etag/hash)**, **через namespace (prefix invalidation)** и **по стратегиям записи (write-through / write-around / write-back)**.
> 
> В Т-Банке мы используем комбинацию этих подходов:  
> TTL — для справочных и быстро обновляемых данных,  
> event-driven invalidation — для синхронизации между микросервисами через Kafka,  
> и namespace-инвалидацию — для массового сброса кэша при обновлении модели данных.

---

## 🔹 1. TTL (Time To Live)

Самый простой и распространённый способ.  
Каждый ключ живёт ограниченное время, после чего автоматически удаляется.

`r.set("client:123", "data", ex=300)  # живёт 5 минут`

✅ Простая реализация.  
❌ Возможна **несвежая информация** до истечения TTL.

🧠 **Пример из Т-Банка:**  
скоринговый результат хранится с TTL = 5 мин, чтобы не пересчитывать одно и то же.

---

## 🔹 2. Явная (explicit) инвалидация по ключу

Сервис сам знает, какой кэш стал неактуальным, и удаляет или обновляет конкретный ключ.

`cache.delete(f"user:{user_id}")`

✅ Прямой контроль над консистентностью.  
❌ Требует дисциплины и знания, какие ключи связаны с изменением.

🧠 **Пример из Т-Банка:**  
при изменении профиля клиента (`client_update`) CRM вызывает `cache.delete(client:{id})` в Redis,  
чтобы другие сервисы при следующем обращении взяли свежие данные из PostgreSQL.

---

## 🔹 3. Инвалидация по событиям (Event-driven)

Инвалидация по **событию из брокера** (Kafka, RabbitMQ, NATS и т.д.).  
Когда данные в БД обновляются — сервис публикует событие, и другие сервисы реагируют.

`# publisher (CRM) event = {"type": "client_update", "client_id": 123} kafka_producer.send("cache_invalidate", event)  # consumer cache.delete(f"client:{event['client_id']}")`

✅ Высокая консистентность между сервисами.  
✅ Масштабируется.  
❌ Нужна надёжная доставка событий (Kafka, retry, idempotency).

🧠 **Пример из Т-Банка:**

> при обновлении клиентских данных CRM публикует событие `client_update`,  
> и микросервисы скоринга, лимитов и уведомлений инвалидируют свои кэши.
> 
> Это обеспечивает _near real-time_ консистентность без коротких TTL.

---

## 🔹 4. Инвалидация по namespace (prefix / generation)

Вместо удаления каждого ключа по отдельности, можно “сменить поколение” namespace-а.

Например:

`# ключ хранится как client:v2:123 r.set("client:v2:123", data)  # инвалидация — смена generation cache_version["client"] = "v3"`

Теперь при следующем запросе все старые `v2` ключи неактуальны — можно не трогать их явно.

✅ Очень быстро, не нужно обходить тысячи ключей.  
❌ Нужно правильно проектировать формат ключей.

🧠 **Пример из Т-Банка:**  
при смене модели данных скоринга (новые поля, формулы) — просто повышаем версию namespace-а:  
все старые кэши становятся невалидными автоматически.

---

## 🔹 5. Инвалидация по версии данных (ETag / Hash / Versioning)

Вместо удаления — сравниваем версии данных.  
Если версия в БД выше, чем в кэше — обновляем.

`cached_version = cache.get("user:123:version") db_version = get_db_version(123) if db_version > cached_version:     cache.delete("user:123")`

✅ Минимум перезаписей, актуальность по версии.  
❌ Требует поддержки версии в данных.

🧠 **Пример из Т-Банка:**  
для профиля клиента хранится `version` — при каждом изменении повышается.  
Если версия в кэше меньше, чем в БД — клиентский кэш автоматически инвалидируется.

---

## 🔹 6. Инвалидация при записи (write-through / write-back / write-around)

Инвалидация может быть частью **стратегии записи**:

|Стратегия|Механика|Как влияет|
|---|---|---|
|**Write-Through**|Пишем сразу в кэш и БД|Кэш всегда актуален|
|**Write-Back**|Пишем в кэш, БД обновляется асинхронно|Быстро, но возможна устарелость|
|**Write-Around**|Пишем только в БД, кэш обновится при следующем чтении|Меньше нагрузки на кэш|

🧠 **Пример из Т-Банка:**  
балансы и лимиты — _write-through_ (всегда согласовано),  
скоринг и аналитика — _write-back_ (асинхронное обновление, скорость важнее точности).

---

## 🔹 7. Комбинированные подходы

В продакшене обычно смешивают методы:

- **TTL + event-driven** — обновление по событиям, а TTL как “страховка”.
    
- **namespace + explicit** — массовая и точечная очистка.
    
- **write-through + Kafka invalidate** — запись + рассылка обновлений.
    

---

## 🔍 Как это выглядит в продакшене (пример из Т-Банка)

> В Т-Банке у нас есть кэш профиля клиента:
> 
> - TTL: 10 минут;
>     
> - при `UPDATE` в CRM → Kafka событие `client_update`;
>     
> - все сервисы инвалидируют Redis-ключ `client:{id}`;
>     
> - при получении нового запроса — данные перечитываются из PostgreSQL и обновляют кэш;
>     
> - в случае массовых изменений (релиз новой схемы данных) — version bump `v3 → v4`.
>     
> 
> Это даёт консистентность в течение 1–2 секунд без перегрузки Redis.

---

## 💬 Как можно красиво закончить ответ

> В целом, способов инвалидации кэша несколько,  
> и выбор зависит от требований к **скорости, консистентности и частоте обновлений**.
> 
> TTL — базовый вариант, события — продакшн-уровень,  
> namespace и versioning — удобны при масштабных изменениях.
> 
> В Т-Банке мы используем гибрид: TTL как fallback, Kafka для событий,  
> и версионирование namespace-ов для массовых сбросов без нагрузки.