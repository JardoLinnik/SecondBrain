Через плагин pytest-cov, который интегрируется с pytest и считает покрытие по формуле:  
  
Coverage (строчное) = (Количество строк кода, которые были выполнены хотя бы раз) / (Общее количество строк кода) * 100%