> **Последовательности (sequences)** в Python — это **упорядоченные коллекции элементов**,  
> у которых можно обращаться к элементам **по индексу** и итерироваться **в определённом порядке**.

---

## ⚙️ Основные свойства последовательностей

1. **Порядок элементов сохраняется**  
    — элементы имеют индекс: `0, 1, 2, ...`
    
2. **Можно обращаться по индексу**
    
    `my_list = [10, 20, 30] print(my_list[1])  # 20`
    
3. **Можно итерироваться в цикле**
    
    `for x in my_list:     print(x)`
    
4. **Поддерживаются операции срезов (slices)**
    
    `print(my_list[0:2])  # [10, 20]`
    
5. **Есть встроенные функции и операторы:**  
    `len()`, `min()`, `max()`, `in`, `+`, `*` и т.д.
    

---

## 📚 Примеры последовательностей в Python

|Тип|Пример|Изменяемость|
|---|---|---|
|`list`|`[1, 2, 3]`|изменяемая|
|`tuple`|`(1, 2, 3)`|неизменяемая|
|`str`|`"hello"`|неизменяемая|
|`range`|`range(5)`|неизменяемая|
|`bytes`, `bytearray`|бинарные последовательности|частично изменяемые|

---

## 🧠 Как можно сказать на собеседовании

> Последовательности — это упорядоченные коллекции элементов,  
> где каждый элемент имеет индекс и порядок важен.
> 
> В Python к ним относятся `list`, `tuple`, `str`, `range` и некоторые другие.
> 
> Они поддерживают обращение по индексу, срезы, итерации и функции вроде `len()`.
> 
> Основное различие — в изменяемости: списки можно менять, а строки и кортежи — нет.