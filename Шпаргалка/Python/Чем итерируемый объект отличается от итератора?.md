> **Итерируемый объект (iterable)** — это **объект, по которому можно пройтись в цикле `for`**.
> 
> **Итератор (iterator)** — это **объект, который реально выполняет обход элементов** — по одному за раз.

---

## ⚙️ Пример

`numbers = [10, 20, 30]   # это итерируемый объект it = iter(numbers)        # это итератор  print(next(it))  # 10 print(next(it))  # 20 print(next(it))  # 30`

👉 `numbers` — **итерируемый объект**, потому что из него можно получить итератор через `iter()`.  
👉 `it` — **итератор**, потому что из него можно доставать элементы через `next()`.

---

## 🧠 Главное отличие

|Характеристика|Iterable (итерируемый)|Iterator (итератор)|
|---|---|---|
|Что это|Контейнер данных|Объект для обхода контейнера|
|Пример|`list`, `tuple`, `str`, `dict`, `set`, `range`|Результат `iter(list)`|
|Есть метод|`__iter__()`|`__iter__()` и `__next__()`|
|Можно использовать несколько раз|✅ Да|❌ Нет (исчерпывается)|
|Пример использования|`for x in data:`|`next(it)`|

---

## 💬 Как можно сказать это на собеседовании

> Итерируемый объект — это объект, который можно передать в `iter()`, чтобы получить итератор.
> 
> Итератор — это уже объект, который знает, **как последовательно выдавать элементы** при вызове `next()`.
> 
> Например, список — это итерируемый объект, а результат `iter(список)` — итератор.
> 
> Цикл `for` внутри Python как раз создаёт итератор и вызывает у него `next()` до тех пор, пока не выбросится `StopIteration`.

---

## 💡 Короткий ответ (если нужно в 20 секунд)

> Итерируемый объект — это то, по чему можно пройтись в цикле.  
> Итератор — это тот, кто реально делает этот проход, выдавая элементы по одному через `next()`.
> 
> Любой итерируемый объект можно превратить в итератор с помощью `iter()`.