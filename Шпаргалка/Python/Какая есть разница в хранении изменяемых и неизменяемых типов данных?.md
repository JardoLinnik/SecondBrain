При изменении неизменяемых объектов создаётся новый объект в памяти в новой ячейке, а при изменении изменяемого объекта ячейка и объект остаются теми же. Это можно проверить с помощью функции id(), которая возвращает уникальный идентификатор объекта.