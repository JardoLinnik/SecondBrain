Если вставка в конец, то О(1) в лучшем и среднем случае и О(n) в худшем, если заканчиваются ячейки и массив переносится в новую область памяти. Если в середину или начало, то О(n), т.к. при вставке элемента в начало или в середину элементы сдвигаются.