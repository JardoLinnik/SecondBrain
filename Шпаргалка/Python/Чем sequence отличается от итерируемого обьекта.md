

|                            | **Последовательность (sequence)**                            | **Итерируемый объект (iterable)**                            |
| -------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| Что это                    | **Упорядоченная коллекция элементов**, у которых есть индекс | **Любой объект, по которому можно пройтись в цикле `for`**   |
| Есть ли порядок            | ✅ Да — элементы идут в чётком порядке                        | ❌ Не обязательно (например, `set` — неупорядочен)            |
| Есть индексация (`obj[i]`) | ✅ Да                                                         | ❌ Необязательно                                              |
| Есть длина (`len(obj)`)    | ✅ Обычно да                                                  | ❌ Необязательно                                              |
| Примеры                    | `list`, `tuple`, `str`, `range`                              | `list`, `tuple`, `set`, `dict`, `file`, `generator`, `range` |
| Всегда iterable?           | ✅ Да, любая последовательность итерируема                    | ❌ Не каждый iterable — последовательность                    |



---

## ⚙️ Примеры

```
# ✅ Это последовательность (итерируемая, с индексом)
nums = [10, 20, 30]
print(nums[1])  # 20
for n in nums:
    print(n)

# ✅ Это итерируемый, но не последовательность (порядка нет)
s = {10, 20, 30}
print(20 in s)  # True
# print(s[1])  # ❌ Ошибка! У set нет индексов

```
---

## 🧠 Как запомнить просто

> 🔹 **Все последовательности — итерируемые**,  
> 🔹 но **не все итерируемые — последовательности**.

---

## 💬 Как сказать это на собеседовании

> Последовательность — это упорядоченный итерируемый объект, у которого есть индексы и фиксированный порядок элементов.
> 
> Например, `list`, `tuple`, `str`.
> 
> Итерируемый объект — понятие шире: это всё, по чему можно пройтись в цикле `for`.
> 
> Например, `set`, `dict`, файлы или генераторы — это итерируемые объекты, но не последовательности, потому что у них нет индексов и порядка.