## **`cat`** — просто вывести весь файл в консоль

`cat filename.txt`

📤 Выведет весь файл “как есть”.

🔹 Пример:

`cat /var/log/syslog`

> ⚠️ Если файл большой — всё вывалится сразу, неудобно читать.

---

## 🧩 2. **`less`** — удобно просматривать файл постранично

`less filename.txt`

📖 Можно:

- прокручивать стрелками вверх/вниз,
    
- искать (`/` + текст),
    
- выйти клавишей `q`.
    

🔹 Пример:

`less /var/log/nginx/access.log`

> 💡 Самый удобный вариант для больших файлов (логи, конфиги и т.д.)

---

## 🧩 3. **`more`** — постраничный просмотр (старый аналог `less`)

`more filename.txt`

Похож на `less`, но проще: листать можно только вниз.

---

## 🧩 4. **`head`** — показать первые строки файла

`head filename.txt`

По умолчанию покажет 10 строк.  
Можно задать количество:

`head -n 20 filename.txt`

---

## 🧩 5. **`tail`** — показать последние строки файла

`tail filename.txt`

Полезно для логов.  
Можно “следить” за изменениями в реальном времени:

`tail -f /var/log/syslog`

📡 `-f` (“follow”) — следит за появлением новых строк в файле.

---

## 🧩 6. **`nl`** — вывести файл с номерами строк

`nl filename.txt`

---

## 🧩 7. **`grep`** — найти нужное слово или строку

`grep "ошибка" app.log`

Можно использовать вместе с `tail` или `cat`:

`tail -n 50 app.log | grep "ERROR"`

---

## 💡 Краткая шпаргалка

|Команда|Назначение|
|---|---|
|`cat file`|показать весь файл|
|`less file`|удобный просмотр постранично|
|`head file`|первые 10 строк|
|`tail file`|последние 10 строк|
|`tail -f file`|“следить” за файлом|
|`grep "строка" file`|поиск по содержимому|

---

## 💬 Как сказать это на собеседовании

> Чтобы посмотреть содержимое файла в Linux, чаще всего используют `cat` для небольших файлов и `less` для больших.
> 
> Для логов удобно использовать `tail -f`, чтобы следить за изменениями в реальном времени.
> 
> Если нужно что-то найти — комбинирую с `grep`, например:  
> `tail -f app.log | grep "ERROR"`.