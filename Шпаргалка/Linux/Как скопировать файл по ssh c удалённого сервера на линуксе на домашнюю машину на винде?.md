## **Через `scp` (Secure Copy Protocol)** — самый простой способ

Если у тебя на Windows стоит **WSL (Linux в Windows)** или **Git Bash**,  
то команда такая же, как и на Linux:

`scp username@remote_host:/path/to/file.txt C:\Users\YourName\Downloads\`

🔹 Пример:

`scp user@192.168.1.10:/home/user/logs/app.log C:\Users\Yaroslav\Desktop\`

> 💡 **Пояснение:**
> 
> - `user@192.168.1.10` — SSH-доступ к серверу
>     
> - `/home/user/logs/app.log` — путь к файлу на сервере
>     
> - `C:\Users\Yaroslav\Desktop\` — путь, куда скопировать файл на Windows
>     

> ⚠️ Работает, если у тебя настроен SSH-доступ (по паролю или ключу).

---

## 🧩 2. **Через WinSCP** (удобный GUI-интерфейс)

Если не хочешь возиться с консолью:

1. Скачай [**WinSCP**](https://winscp.net/eng/download.php)
    
2. При запуске выбери:
    
    - **Протокол:** SFTP (по SSH)
        
    - **Host name:** IP-адрес сервера
        
    - **Username / Password** (или ключ)
        
3. После подключения откроется окно с двумя панелями —  
    левая это твой ПК, правая — сервер.
    
4. Просто **перетащи файл мышкой** с сервера на свой компьютер.
    

> 💡 Отличный вариант, если хочешь видеть всё наглядно и не писать команды.

---

## 🧩 3. **Через `rsync`** (если есть WSL)

Если у тебя стоит **WSL (Windows Subsystem for Linux)**,  
можно использовать мощную Linux-команду `rsync`:

`rsync -avz user@192.168.1.10:/home/user/data/ ./data/`

📦 Она:

- копирует только изменённые файлы,
    
- сохраняет права доступа,
    
- может работать инкрементально.
    

---

## 🧩 4. **Если у тебя PowerShell (без WSL)**

Можно использовать встроенный `scp`, если установлен **OpenSSH-клиент** (Windows 10+):

`scp user@192.168.1.10:/home/user/file.txt C:\Users\Yaroslav\Desktop\`

Проверить, установлен ли SSH:

`Get-WindowsCapability -Online | Where-Object Name -like 'OpenSSH*'`

Если нет — установи:

`Add-WindowsCapability -Online -Name OpenSSH.Client~~~~0.0.1.0`

---

## 🧠 Коротко: что выбрать

| Условия                                   | Лучший вариант                |
| ----------------------------------------- | ----------------------------- |
| Любишь консоль и есть WSL / Git Bash      | ✅ `scp`                       |
| Хочешь визуально — drag & drop            | ✅ WinSCP                      |
| Нужно регулярно синхронизировать каталоги | ✅ `rsync`                     |
| Только PowerShell под рукой               | ✅ встроенный `scp` из OpenSSH |