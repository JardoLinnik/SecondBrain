> **Gunicorn** — это **WSGI-сервер** (Web Server Gateway Interface) для Python-приложений.
> 
> Он нужен, чтобы **запускать Django или Flask в продакшене** и принимать HTTP-запросы не напрямую, а через веб-сервер (обычно Nginx).

---

## ⚙️ Зачем нужен Gunicorn

Дело в том, что:

- Приложение на Django или Flask — это просто Python-код.
    
- Оно **не умеет само по себе** принимать и обслуживать множество HTTP-запросов из сети.
    
- Gunicorn — это как “прослойка” между веб-сервером (Nginx) и твоим Python-кодом.
    

Он:

1. Принимает запросы от Nginx.
    
2. Передаёт их в Python-приложение (через WSGI-протокол).
    
3. Возвращает ответ обратно в Nginx, который отдаёт его пользователю.
    

---

## ⚙️ Как это работает

`Пользователь → Nginx → Gunicorn → Django/FastAPI → База данных`

- **Nginx** принимает внешний трафик (HTTP/HTTPS).
    
- **Gunicorn** общается с приложением на уровне Python (через сокеты).
    
- **Приложение** отвечает за бизнес-логику.
    

---

## 🧩 Почему нельзя просто запустить Django командой `runserver`

> Команда `python manage.py runserver` — это встроенный _dev-сервер_,  
> он предназначен только для локальной разработки:
> 
> - плохо работает с параллельными запросами,
>     
> - не перезапускается при падениях,
>     
> - не масштабируется.
>     

**Gunicorn**, в отличие от него:

- умеет работать с несколькими процессами и потоками;
    
- перезапускает воркеры при сбоях;
    
- устойчив к высокой нагрузке;
    
- даёт стабильное поведение в продакшене.
    

---

## ⚙️ Пример запуска Gunicorn

`gunicorn myapp.wsgi:application --workers 4 --bind 0.0.0.0:8000`

- `myapp.wsgi:application` — точка входа Django-приложения
    
- `--workers 4` — количество процессов (воркеров), которые обрабатывают запросы
    
- `--bind` — адрес, на котором слушает сервер
    

---

## 💬 Как сказать это на собеседовании

> Gunicorn — это WSGI-сервер, который используется для запуска Python-приложений в продакшене.
> 
> Он принимает запросы от Nginx и передаёт их в Django или Flask.  
> Благодаря многопроцессной модели Gunicorn умеет обрабатывать много запросов параллельно, перезапускает воркеры при сбоях и обеспечивает стабильную работу приложения.
> 
> В паре с Nginx он образует стандартную схему деплоя: Nginx — внешняя точка входа, Gunicorn — Python-сервер, приложение — бизнес-логика.