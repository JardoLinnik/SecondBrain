> **Certbot** — это инструмент, который автоматически получает и обновляет **SSL-сертификаты** от **Let’s Encrypt**.
> 
> Он нужен, чтобы сделать твой сайт или API доступным по **HTTPS**, а не по обычному HTTP.

---

## 🔒 Зачем нужен Certbot

Когда ты настраиваешь HTTPS:

- тебе нужен **SSL-сертификат**, чтобы шифровать соединение между пользователем и сервером;
    
- обычно сертификаты платные и требуют ручной настройки;
    
- **Let’s Encrypt** — это бесплатный центр сертификации,  
    а **Certbot** — это программа, которая берёт и устанавливает эти сертификаты автоматически.
    

---

## ⚙️ Как работает Certbot

1. Certbot связывается с **Let’s Encrypt**.
    
2. Подтверждает, что ты владеешь доменом (например, `myapp.example.com`).
    
3. Получает оттуда **сертификат (файлы .pem)**.
    
4. Устанавливает сертификат в **Nginx** или **Apache**.
    
5. Настраивает **автоматическое продление** (обычно раз в 60–90 дней).
    

---

## 🧩 Пример настройки (на Nginx)

`sudo apt install certbot python3-certbot-nginx sudo certbot --nginx -d myapp.example.com`

После этого Certbot:

- настроит HTTPS в конфиге Nginx;
    
- перезапустит сервер;
    
- и автоматически создаст задачу (`cron`) для продления сертификата.
    

Проверить продление можно командой:

`sudo certbot renew --dry-run`

---

## 🧠 Как можно сказать это на собеседовании

> Certbot — это утилита для автоматического получения и обновления SSL-сертификатов от Let’s Encrypt.
> 
> Мы используем его, чтобы быстро настроить HTTPS для наших сервисов.
> 
> Он интегрируется с Nginx: получает сертификат, добавляет конфигурацию и продлевает его автоматически,  
> так что нам не нужно вручную заниматься сертификатами и безопасностью соединения.