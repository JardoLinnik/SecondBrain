> **Gunicorn** — это **WSGI-сервер**,  
> а **Uvicorn** — это **ASGI-сервер**.
> 
> Разница в том, **какие приложения они умеют запускать**:
> 
> - Gunicorn — синхронные (например, **Django**, **Flask**)
>     
> - Uvicorn — асинхронные (например, **FastAPI**, **Starlette**, **Django 3.0+ с ASGI**)
>     

---

## ⚙️ Что это значит

### 🧱 Gunicorn

- Работает по старому стандарту **WSGI (Web Server Gateway Interface)**.
    
- Он обрабатывает **по одному запросу на поток/воркер**, т.е. синхронно.
    
- Идеален для Django или Flask, где нет `async def`.
    
- Не умеет напрямую работать с WebSocket’ами и асинхронными задачами.
    

🔧 Пример:

`gunicorn myapp.wsgi:application --workers 4`

---

### ⚡ Uvicorn

- Работает по стандарту **ASGI (Asynchronous Server Gateway Interface)**.
    
- Поддерживает **асинхронные функции**, `async/await`, WebSocket, Server-Sent Events.
    
- Используется с FastAPI, Starlette и современными версиями Django.
    
- Легковесный, быстрый и идеально подходит для real-time API.
    

🔧 Пример:

`uvicorn app.main:app --host 0.0.0.0 --port 8000`

---

## ⚙️ Gunicorn + Uvicorn — вместе

> В продакшене их часто **используют вместе**:  
> Gunicorn управляет процессами, а Uvicorn обрабатывает асинхронные запросы.

`gunicorn app.main:app \   -k uvicorn.workers.UvicornWorker \   --workers 4`

- Здесь `-k uvicorn.workers.UvicornWorker` говорит Gunicorn использовать Uvicorn как воркер.
    
- Это даёт стабильность Gunicorn + скорость Uvicorn.
    

---

## 💬 Как сказать это на собеседовании

> Gunicorn и Uvicorn — это оба Python-сервера, но они работают по разным протоколам.  
> Gunicorn — WSGI, для синхронных фреймворков вроде Django и Flask.  
> Uvicorn — ASGI, для асинхронных, таких как FastAPI.
> 
> Если нужно запустить асинхронное приложение в проде, мы обычно используем Gunicorn как менеджер процессов,  
> а внутри — Uvicorn-воркеры.  
> Это даёт и стабильность, и поддержку async-запросов.