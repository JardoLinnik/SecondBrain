Используется специальная кодировка, которая спецсимволам сопоставляет специальный код, например символ & кодируется как %26. Кодировать необходимо, если мы передаём спецсимвол в строке запроса либо в header. При передаче в теле запроса кодировать символ не нужно.