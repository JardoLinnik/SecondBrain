> **Балансировщик нагрузки (load balancer)** — это компонент, который **распределяет входящие запросы между несколькими серверами или контейнерами**, чтобы ни один из них не был перегружен.
> 
> Его цель — сделать систему **быстрее, надёжнее и отказоустойчивее**.

---

## ⚙️ Зачем он нужен

1. Если у тебя один сервер — он может не выдержать много запросов.
    
2. Если сервер упадёт — пользователи не смогут получить доступ.
    
3. Балансировщик решает эти проблемы:
    
    - распределяет трафик по нескольким серверам,
        
    - убирает из пула “падающие” инстансы,
        
    - позволяет масштабировать приложение горизонтально.
        

---

## 🔁 Пример

`Пользователь → Nginx (балансировщик)              ↙         ↓         ↘        backend-1   backend-2   backend-3`

> Пользователь не знает, к какому именно серверу попал —  
> Nginx или другой балансировщик решает это автоматически по алгоритму.

---

## ⚙️ Основные **алгоритмы балансировки нагрузки**

|Алгоритм|Как работает|Когда применять|
|---|---|---|
|**Round Robin**|Запросы идут по кругу: 1 → 2 → 3 → снова 1|Простой и равномерный случай|
|**Least Connections**|Новый запрос идёт на сервер с **меньшим количеством активных соединений**|Если нагрузка по запросам неравномерная|
|**IP Hash**|Балансировщик выбирает сервер на основе **IP-адреса клиента**|Когда нужно, чтобы один клиент всегда попадал на один и тот же сервер (например, сессии)|
|**Weighted Round Robin**|У каждого сервера есть “вес” — более мощные получают больше запросов|Когда сервера разные по мощности|
|**Random**|Выбирается случайный сервер|Простой и иногда эффективный при высокой нагрузке|
|**Least Response Time**|Запрос идёт на сервер с **наименьшей средней задержкой**|Когда важна скорость отклика|

---

## 🧱 Где балансировщик может стоять

- **Перед приложением (L7)** — например, Nginx, HAProxy, Traefik (балансировка по URL, заголовкам, кукам и т.д.)
    
- **На уровне сети (L4)** — например, AWS ELB, Google Load Balancer, MetalLB (TCP/UDP, IP/порт).
    
- **В Kubernetes** — встроенный балансировщик через `Service` (ClusterIP, NodePort, LoadBalancer).
    

---

## 💬 Как можно сказать это на собеседовании

> Балансировщик нагрузки распределяет запросы между несколькими инстансами приложения,  
> чтобы обеспечить стабильную работу, масштабирование и отказоустойчивость.
> 
> Чаще всего используется схема Nginx + несколько backend-экземпляров.
> 
> Основные алгоритмы: Round Robin, Least Connections, IP Hash и Weighted Round Robin.  
> Мы, например, в Т-Банке использовали **Least Connections**, потому что запросы к сервисам занимали разное время,  
> и нужно было равномерно распределять нагрузку не по количеству, а по “занятости” инстансов.