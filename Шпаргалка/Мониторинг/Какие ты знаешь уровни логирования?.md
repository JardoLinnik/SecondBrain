> Основные уровни логирования — это **DEBUG**, **INFO**, **WARNING**, **ERROR** и **CRITICAL**.
> 
> Мы используем их в Т-Банке строго по назначению, чтобы логи оставались осмысленными и не забивали хранилище.
> 
> Например, `INFO` — это успешные бизнес-события, `WARNING` — нештатные, но восстановимые ситуации (ретраи, таймауты), `ERROR` — реальные ошибки в логике или интеграциях, а `CRITICAL` — фатальные сбои, после которых сервис требует вмешательства.
> 
> `DEBUG` включаем только выборочно, по feature-флагу или trace-id, чтобы не перегружать прод.

---

## 🔹 Основные уровни логов

|Уровень|Когда использовать|Пример|
|---|---|---|
|🐞 **DEBUG**|Подробная отладочная информация, полезна только разработчикам|«Запрос к antifraud-service, payload=..., response=...»|
|ℹ️ **INFO**|Нормальное поведение системы, бизнес-события|«Платёж успешно проведён», «Сессия клиента создана»|
|⚠️ **WARNING**|Нештатная, но не критичная ситуация|«Не удалось получить кеш, идём в БД», «Retry №2»|
|❌ **ERROR**|Ошибка в бизнес-логике или внешней системе, но процесс жив|«Timeout при вызове scoring-service», «Ошибка записи в PostgreSQL»|
|💥 **CRITICAL / FATAL**|Фатальная ошибка, после которой сервис не может работать|«Kafka consumer не запущен», «БД недоступна — завершаем процесс»|

---

## 🔹 Пример в коде (FastAPI, logging)

`import logging log = logging.getLogger("payments")  try:     payment = process_payment(order_id)     log.info("payment_success", extra={"order_id": order_id, "user_id": user_id}) except TimeoutError as e:     log.warning("payment_retry_timeout", extra={"order_id": order_id, "retry": 2}) except Exception as e:     log.error("payment_failed", exc_info=e, extra={"order_id": order_id})`

> При этом `DEBUG`-логи у нас в проде по умолчанию отключены —  
> их можно включить для конкретного `trace_id` или в dev/staging окружении.

---

## 🔹 Как используется в продакшене (Т-Банк)

> В продакшене мы пишем только `INFO`, `WARNING`, `ERROR`, `CRITICAL` — все логи структурированы и уходят в Loki.
> 
> `DEBUG` включаем точечно, например, при расследовании инцидента через фиче-флаг или trace-id-фильтр.
> 
> `ERROR` и `CRITICAL` автоматически дублируются в **Sentry**, а `WARNING` и `ERROR` агрегируются в метрики Grafana — это помогает строить алерты и отслеживать SLA.

---

## 🔹 Пример как это влияет на наблюдаемость

|Уровень|Куда идёт|Реакция|
|---|---|---|
|DEBUG|Только Dev/Staging|Без алертов|
|INFO|Loki / ELK|Для аналитики и аудита|
|WARNING|Loki + Prometheus metrics|Может триггерить soft alert|
|ERROR|Loki + Sentry|Алерт на duty-инженера|
|CRITICAL|Loki + PagerDuty / Alertmanager|Немедленное уведомление on-call команды|

---

## 💬 Как можно завершить ответ

> Если коротко, уровни логов — это **система приоритетов и фильтров**.  
> Они помогают отделить важное от второстепенного, и позволяют системам вроде Grafana и Sentry правильно реагировать на события.
> 
> В Т-Банке мы строго следим за этим: отладка — только DEBUG, нормальные события — INFO, проблемы — WARNING/ERROR, аварии — CRITICAL.