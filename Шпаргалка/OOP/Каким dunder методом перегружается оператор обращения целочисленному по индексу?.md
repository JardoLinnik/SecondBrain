> Оператор **обращения по индексу** (`obj[index]`) перегружается с помощью метода  
> 👉 **`__getitem__(self, index)`**

---

## ⚙️ Пример

`class MyList:     def __init__(self, data):         self.data = data      def __getitem__(self, index):         print(f"Получаем элемент с индексом {index}")         return self.data[index]  nums = MyList([10, 20, 30]) print(nums[1])`

📤 Вывод:

`Получаем элемент с индексом 1 20`

---

## 🔧 Дополнительно

В Python есть ещё два связанных метода:

|Метод|Что делает|Когда вызывается|
|---|---|---|
|`__getitem__(self, index)`|Получение элемента|`x = obj[index]`|
|`__setitem__(self, index, value)`|Изменение элемента|`obj[index] = x`|
|`__delitem__(self, index)`|Удаление элемента|`del obj[index]`|

---

## 💬 Как можно сказать на собеседовании

> Оператор обращения по индексу перегружается методом `__getitem__`.  
> Когда мы пишем `obj[i]`, Python внутри вызывает `obj.__getitem__(i)`.
> 
> Если нужно поддержать изменение и удаление по индексу —  
> можно дополнительно реализовать `__setitem__` и `__delitem__`.