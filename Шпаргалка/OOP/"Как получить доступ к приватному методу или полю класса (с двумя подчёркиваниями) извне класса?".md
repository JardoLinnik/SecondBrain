```  
class MyClass:  
def __init__(self):  
self.__hidden_variable = "Secret"  
  
obj = MyClass()  
print(obj._MyClass__hidden_variable) # Выведет: "Secret"  
```  
  
С методами аналогично, но надо сделать вызов через ().