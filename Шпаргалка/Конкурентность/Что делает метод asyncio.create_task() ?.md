> **`asyncio.create_task()`** — создаёт **задачу (task)** из корутины и **запускает её выполнение в фоне**.
> 
> То есть корутина начинает выполняться **параллельно** с другими задачами в том же event loop.

---

## ⚙️ Пример

```
import asyncio

async def say_hello():
    print("Привет!")
    await asyncio.sleep(1)
    print("Пока!")

async def main():
    task = asyncio.create_task(say_hello())  # запускаем в фоне
    print("Задача создана")
    await asyncio.sleep(2)
    print("main завершён")

asyncio.run(main())

```

📤 Вывод:

`Задача создана Привет! Пока! main завершён`

---

## 🧠 Что происходит под капотом

Когда ты вызываешь `asyncio.create_task(coroutine())`:

1. Python **оборачивает корутину в Task-объект** (экземпляр `asyncio.Task`);
    
2. Добавляет задачу в **текущий event loop**;
    
3. Она **начинает выполняться немедленно**, независимо от других `await`.
    

> Важно: чтобы Task выполнилась до конца, нужно **где-то её дождаться** (например, `await task`),  
> иначе программа может завершиться раньше, чем задача закончит работу.

---

## ⚙️ Пример с несколькими задачами

```
import asyncio

async def worker(name, delay):
    await asyncio.sleep(delay)
    print(f"{name} завершился")

async def main():
    task1 = asyncio.create_task(worker("A", 2))
    task2 = asyncio.create_task(worker("B", 1))

    print("Обе задачи запущены")
    await task1
    await task2
    print("Все задачи завершены")

asyncio.run(main())

```
📤 Вывод:

`Обе задачи запущены B завершился A завершился Все задачи завершены`

🧩 Обе корутины выполняются **параллельно (в рамках одного потока)**.

---

## 💡 Когда использовать

✅ Когда нужно **запустить несколько корутин одновременно**;  
✅ Когда нужно **сделать фоновую задачу** (например, логирование, обновление кэша);  
✅ Когда ты хочешь **запустить корутину, но не блокировать текущий код**.

---

## ⚠️ Важно помнить

- `asyncio.create_task()` можно вызывать **только внутри асинхронного контекста** (то есть внутри `async def`).
    
- Если ты не дождёшься выполнения задачи (`await task`),  
    Python может выдать предупреждение:
    
    `Task was destroyed but it is pending!`
    

---

## 💬 Как можно сказать на собеседовании

> `asyncio.create_task()` создаёт и запускает корутину в виде задачи в event loop.
> 
> В отличие от `await`, который блокирует выполнение до завершения,  
> `create_task()` позволяет запустить корутину параллельно с другими.
> 
> Это удобно, когда нужно выполнять несколько операций одновременно — например,  
> параллельно отправлять запросы или читать данные из разных источников.