У корутины, являющейся частным случаем генератора, вызывается dunder-метод __await__, который под капотом использует конструкцию yield from. Далее Event Loop переключает выполнение корутин в точках await по мере их готовности к выполнению (см. механизм мультиплексирования)